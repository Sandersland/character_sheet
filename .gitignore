node_modules/
dist/
build/
coverage/
.env
# Vite's mode-scoped variants, generated per worktree slot (#1458).
.env.development
.env.production
*.log
.DS_Store
backend/src/generated/

# autodev FSM run ledgers (see .claude/skills/autodev)
.claude/autodev/

# Playwright / verify-tool artifacts
.playwright-mcp/
page-*.png
test-results/
playwright-report/
driver.mjs

# npm workspaces: the only lockfile is the root one
backend/package-lock.json
frontend/package-lock.json
.fallow/
lefthook-local.yml

# Harness runtime state (ephemeral)
.claude/scheduled_tasks.lock
.claude/*.lock
