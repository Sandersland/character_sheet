// Manual-entry floor/ceiling moved to ReferenceData.abilityGeneration.manual
// (PHB'14 p.13 / SRD 5.2, validateAbilityScores enforces server-side) — no
// local mirror lives here (#1383).

export const CHIP_BASE =
  "rounded-control border px-3 py-1.5 text-xs font-semibold transition-colors";
