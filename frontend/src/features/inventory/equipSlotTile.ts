// Shared tile chrome for the paper-doll's three EquipSlotCell render branches
// (locked, filled, empty) so their box-model stays pixel-identical.
export const TILE =
  "flex aspect-square w-full flex-col items-center justify-center gap-1 rounded-card border p-1 text-center transition-colors";
