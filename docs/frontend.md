# Frontend guidelines

## Directory structure — where things belong

Source of truth: `ls frontend/src/features` — regenerate if stale.

```
frontend/src/
├── components/
│   └── ui/              # domain-agnostic primitives (Card, Badge, MeterBar, Modal, BottomSheet, Tabs, OverflowMenu, DropdownMenu, Popover, Avatar, ErrorBoundary, EmptyState, Spinner)
├── features/
│   ├── abilities/       # AbilityScoreBox, AbilityScoreEditor, SkillsTable, ProficienciesCard, AbilityScoresPanel,
│   │                    #   ProficientSkillsCard (#923: Overview proficient/expertise summary + "All 18" SkillsTable modal)
│   ├── advancement/     # AdvancementSection, AdvancementPanel (shell) → AsiFlow, FeatFlow,
│   │                    #   CustomFeatForm; hooks useAsiDraft/useFeatCatalog/useCustomFeatDraft; featView reducer
│   ├── auth/            # AuthProvider (useAuth), AuthGate, AppHeader, AccountMenu
│   ├── campaign/        # CampaignsPage (list+create; join is URL-only via /join/:code #520), CampaignDetailPage (mgmt hub with
│   │                    #   routed Overview/Codex + owner-only Manage tabs #367/#379), CampaignOverviewPanel (invite link,
│   │                    #   roster, add-character dropdown), CampaignInviteLink,
│   │                    #   CampaignIndicator (sheet badge/link), JoinCampaignRoute (#246),
│   │                    #   CampaignPreferencesPanel (per-campaign play prefs, sheet-only when attached #537)
│   ├── character-create/ # IdentitySection, AbilityScoresSection, SkillSection,
│   │                    #   ToolProficiencySection + useToolProficiencyChoices,
│   │                    #   StartingEquipmentSection, PreviewSection, CreateActions
│   │                    #   (CharacterCreatePage step frames; orchestrated by hooks/useCharacterCreation)
│   ├── character-meta/  # CharacterCard, BannerVitals, ActivityModal, DeleteCharacterModal, BackendStatus,
│   │                    #   IdentityCard (#927: read-only background/alignment on the Story panel),
│   │                    #   CharacterSheet{Header,Content,Body,Modals}, CharacterLoadError, panels/ (sheet-page sections)
│   │                    #   Tabbed sheet shell (#921/#922): CharacterSheetPage = load/guard states →
│   │                    #   CharacterSheetContent (banner + chrome) → CharacterSheetHeader is the
│   │                    #   persistent garnet banner (identity + BannerVitals + Tabs) and CharacterSheetBody
│   │                    #   routes the ?tab= panel (panels/{Overview,Combat,Inventory,Magic,Story}Panel).
│   │                    #   OverviewPanel (#923): AbilityScoresPanel (abilities + saves row) on top,
│   │                    #   then a 3-col grid — ProficientSkillsCard+Proficiencies / Class Features+Advancements /
│   │                    #   Experience+Spell Slots (SpellSlotSummary, caster-only)+Equipped.
│   │                    #   StoryPanel groups the journal doorway + IdentityCard + campaign prefs (#927).
│   │                    #   Tab logic is pure in sheetTabs.ts (getSheetTabs/resolveActiveTab; Magic is
│   │                    #   caster-only) + the useSheetTabs hook (URL-backed ?tab= state).
│   │                    #   BannerVitals' AC tile is read-only: a Popover disclosing the
│   │                    #   server-derived armorClassBreakdown verbatim (no client AC math)
│   ├── class/           # ClassFeaturesSection, FightingStylePanel, AddManeuverPanel,
│   │                    #   ManeuverRow, ResourcePoolRow, DisciplinesSection,
│   │                    #   DisciplineRow, AddDisciplinePanel (Four Elements monk),
│   │                    #   ShadowArtsSection, ShadowArtRow (Way of Shadow ki-cast),
│   │                    #   CloakOfShadowsSection (Way of Shadow L11 self-invisible toggle),
│   │                    #   AbilityRowShell (#688: the shared expandable-row <li> anatomy —
│   │                    #   name toggle + chips + action cluster — behind the three rows)
│   ├── conditions/      # ConditionsStrip (desktop card) + ConditionsSheetBody (shared innards:
│   │                    #   chips/exhaustion/AddConditionPanel + applyConditionTransactions), AddConditionPanel,
│   │                    #   CompactConditionsBar (mobile session strip; tap → "Conditions" BottomSheet → ConditionsSheetBody, #769)
│   ├── dice/            # DiceRoller, PhysicsDiceRoller, DiceScene, DieMesh, DiceRollSequence,
│   │                    #   DiceRollModal, RollButton, RollContext, RollResultToast, RollModeToggle,
│   │                    #   diceRollerTypes.ts, useDieFaceData.ts
│   ├── entities/        # CampaignCodex (Codex tab: full-width browse shell orchestrating rail + ledger #840),
│   │                    #   CodexRail (search/type-filter/sort/create toggle), CodexLedger (A→Z letter-divided
│   │                    #   rows + alphabet jump rail, flat ranked list for mention sorts #853),
│   │                    #   CodexActivityRail + NeedsChroniclingBanner + useCodexActivity
│   │                    #   (xl activity column + sub-xl gold banner #841),
│   │                    #   EntityCreateForm (shared rail-panel/bottom-sheet create),
│   │                    #   CampaignManagePanel (owner Manage tab:
│   │                    #   identity merges only #379/#523), EntityDetailPage (wiki-article reading pane #842)
│   │                    #     → orchestrator over EntityPaneRail (desktop sibling list) + EntityArticleHeader/
│   │                    #       EntityEditForm/EntityInfobox/EntityChronicle/EntityConnections/EntityContributeBand/
│   │                    #       EntityMergeCards + hooks useEntityDetail/useEntityMerges/useEntityBackTo
│   ├── experience/      # ExperienceTracker
│   ├── hitpoints/       # HitPointTracker orchestrator (inline Card) → presentational sections
│   │                    #   HpDeathSaveBlock / HpNotices (level-up + advancement + concentration + error) /
│   │                    #   HpTrackerModals (LevelUpModal + ConcentrationSaveModal), + useHitPointTrackerActions
│   │                    #   (level-up/advancement state; rest ops via the shared useRestActions) (#779/#814).
│   │                    #   Sub-components: HpActionControl (vertical flow mode → amount →
│   │                    #   projection → chips → type → Apply: Damage/Heal/Temp Segmented + a single
│   │                    #   display-size amount input flanked by −/+ (numeric entry) + an aria-live
│   │                    #   projected-HP line "→ N / max" + accumulator chips +5/+10/+20/Clear +
│   │                    #   full-width Apply echoing the amount, #796;
│   │                    #   Damage-mode-only damage-type picker + resistance auto-halve toggle, #456/#787;
│   │                    #   pure amount math — accumulate/clamp/project — in lib/hpAmount, #787),
│   │                    #   HpMeter, RestControls, DeathSaveTracker, LevelUpCallout, AdvancementCallout;
│   │                    #   CompactHpBar (session strip; tap → "Hit Points" BottomSheet → HpSheetBody, #768),
│   │                    #   RestButton (session campfire button; tap → "Rest" BottomSheet → RestControls, #814),
│   │                    #   useHitPointApply (shared submit + concentration-check surfacing behind the HP
│   │                    #   tracker AND the session sheet; per-mode ops built by lib/hitPointOps buildHpOps, #779),
│   │                    #   useRestActions (shared short/long rest ops behind HitPointTracker + RestButton, #814),
│   │                    #   AutoRollConcentrationToggle + ConcentrationNoteBanner
│   ├── inventory/       # InventoryList orchestrator (Bag ⇄ Worn toggle) → InventoryHeaderActions
│   │                    #   (→ SellModeActions)/InventoryMeters/InventoryToolbar/
│   │                    #   InventoryBody (→ InventoryContent → InventorySections)/
│   │                    #   CurrencyEditor (→ CurrencyEditForm)
│   │                    #   + cluster hooks useItemCatalog/useInventoryTransactions/useSellSelection,
│   │                    #   InventoryRow (→ InventoryEditForm/EquipToggle/AttuneToggle/ItemSummary/
│   │                    #   ItemProse), AddItemPanel, SellPanel,
│   │                    #   StartingEquipmentEditor, LoadoutList (→ SlotPickerPanel/AttuneToggle),
│   │                    #   EquippedItemsCard (#923: read-only Overview list of equipped items by slot)
│   ├── journal/         # JournalDoorway (#867: the sheet's only journal surface — a CSS-drawn closed-book
│   │                    #   card that links to the journal page; no editing on the sheet; summary via
│   │                    #   journalDoorwaySummary.ts). Field-chronicle page cluster (#864): JournalPage's ChronicleSpine (chapter
│   │                    #   spine — sessions as chapters grouped by CampaignArc "parts"; flat list when
│   │                    #   no arcs) + ManuscriptPage (serif prose ENTRY rows w/ drop cap, "Field notes"
│   │                    #   NOTE list, inline composer + "＋ New entry", in-place chapter-title rename).
│   │                    #   Pure spine model in chronicle.ts (buildChronicleSpine/defaultChapterId/
│   │                    #   findChapter/partIdForChapter/filterSpine); data via useChronicle (arcs +
│   │                    #   chronicle sessions). JournalEntryPanel (NOTE default / ENTRY kind long-form),
│   │                    #   MentionText/MentionAutocomplete (inked @-mentions), useJournalMutations
│   │                    #   (shared REST CRUD). CapturePalette (⌘J/Ctrl+J quick-capture NOTE orchestrator —
│   │                    #   full-height keyboard-pinned MobileCaptureSheet below md #866, non-modal
│   │                    #   CaptureDock margin panel at md+ #865) → GrowingComposer (reusable
│   │                    #   pill→ruled-card composer; variant="mobile" swaps the Private checkbox row for
│   │                    #   an inline lock icon-button + ≥44px send) + MobileFeed/DockFeed (both newest-
│   │                    #   at-bottom under day dividers; MobileFeed uses a "Tonight ·" divider). Mobile
│   │                    #   surface tracks visualViewport (useVisualViewport) so the composer docks flush
│   │                    #   above the iOS keyboard; keeps the #784 double-rAF/preventScroll focus
│   │                    #   mitigations; @-mention popover opens ABOVE (never under the keyboard); dark =
│   │                    #   the "Campfire" register via token flips + a dark-only gold glow (index.css)
│   ├── session/         # TurnHub (→ useTurnActions + TurnControls/ActionSlot/BonusActionSlot/
│   │                    #   ReactionSlot/EffectManeuverStrip/LayOnHandsInput + LoadoutRefundStrip (#815)
│   │                    #   + OptionCard (row/half/tile picker cards) →
│   │                    #   ActionSheetBody/BonusActionSheetBody (rich option-card sheets)
│   │                    #   + TurnResolutionSheets → InlineLoadoutPicker (Change weapons #815)
│   │                    #   → InlineAttackPicker (+ InlineSpellAttackSection, Fire Bolt #734) /
│   │                    #   InlineOffHandPicker (TWF off-hand, #732) / …;
│   │                    #   decorative InitiativeRail behind turnFlags, #737;
│   │                    #   read-only TurnConcentrationBanner #735,
│   │                    #   inline TurnDeathSaves 0-HP death-save panel #736),
│   │                    #   useTurnState (turn economy + turn-scoped LIFO undo #730/#758), SessionLog,
│   │                    #   SessionsModal, SessionSummaryModal (→ useSessionRecapDetail +
│   │                    #   CampaignRecapSection/RecapFacts/RecapPrimitives/ParticipantRecapCard/
│   │                    #   SessionJournalList/SessionAddXpForm), Inline{Attack,Item,Spell}Picker, ManeuverPrompt,
│   │                    #   attack sheet (#786/#811): InlineAttackPicker orchestrates one AttackStepCard
│   │                    #   (numbered step rail Roll to hit → Call it → Damage; Segmented "Attacking with"
│   │                    #   selector over buildAttackForms: deduped weapons + Unarmed + Improvised, default
│   │                    #   = main-hand; steps 2–3 bind to the LAST-ROLLED form) + AttackTallyStrip +
│   │                    #   ManeuversDisclosure (44px collapsed row hosting both ManeuverPrompt sections +
│   │                    #   AttackOptionSection) + AttackSheetFooter; md+ = wide BottomSheet with a right
│   │                    #   rail (counter/tally/maneuvers/cantrips) via useIsBelowMd; off-hand/TWF
│   │                    #   (InlineOffHandPicker, #813) reuses the SAME shell — one AttackStepCard over
│   │                    #   [buildOffHandEntry] (segmented selector collapses to "Dagger (off-hand)"), the
│   │                    #   "This bonus action" AttackTallyStrip (source="bonusAction"), ManeuversDisclosure,
│   │                    #   AttackSheetFooter — scoped to the single swing (no Resume/pips);
│   │                    #   per-entry roll state + AttackEntryView from useAttackRolls (verdict = the crit
│   │                    #   authority); useTallyResolve powers the Turn-summary banner's inline resolve;
│   │                    #   shared DamageRiderList; AttackResultLine renders
│   │                    #   lib/attackResult (nat-20 auto-crit + nat-1 miss cues #766), AttackOptionRow,
│   │                    #   useSpellPicker + SpellPickerRow/SlotLevelSelector/SpellTargetToggle,
│   │                    #   EndSessionPrompt, actionResolvers.ts, useActiveResolution, useManeuverDie,
│   │                    #   useSessionButton (sheet-header Start/Join/Resume session state)
│   ├── spells/          # SpellsSection, SpellRow, AddSpellPanel,
│   │                    #   SpellSlotSummary (#923: read-only Overview per-level pips; full mgmt on Magic tab)
│   └── theme/           # ThemeProvider (useTheme) — applies data-theme app-wide
├── hooks/               # reusable React hooks used by pages or multiple clusters
│   │                    #   (useCharacter, useCharacterList, useCharacterDraft, useReferenceData,
│   │                    #    useCharacterCreation, useThemePreference, useCaptureHotkey (⌘J/Ctrl+J toggle
│   │                    #    for the quick-capture dock; ignores dialogs + non-dock inputs, #865), useDismissable)
├── lib/                 # pure TS logic — NO React/JSX (dice, abilities, timeline, startingEquipment,
│   │                    #   entityBacklinks chronicle grouping #842, …)
├── pages/               # route-level views (CharacterListPage, CharacterSheetPage,
│   │                    #   CharacterCreatePage, SessionPage, JournalPage (/characters/:id/journal —
│   │                    #   field chronicle, #864), LoginPage, AboutPage)
├── api/
│   └── client.ts        # the only fetch() call site (apiFetch wrapper: credentials + 401)
├── types/
│   ├── character.ts     # shared domain types
│   └── auth.ts          # AuthUser, AuthProviderInfo
└── test/
    ├── setup.ts         # vitest/jsdom setup (jest-dom + jest-axe matchers + RTL cleanup)
    └── axe.ts           # re-exports jest-axe `axe` + vitest type augmentation
```

### Decision rule — "where does X go?"

Work through this checklist in order; stop at the first match:

1. **Pure logic, no JSX/React** → `lib/` (e.g. `lib/dice.ts`, `lib/abilities.ts`).
2. **A React hook** — used by multiple clusters or by a page → `hooks/`; used only within one feature cluster → co-locate it in that cluster (e.g. `features/dice/useDieFaceData.ts`).
3. **A component with no D&D knowledge** — no imports from `@/types/character`, no `@/api` calls, no game-rule logic, could ship in a different app unchanged → `components/ui/`.
4. **Any other component** → `features/<domain>/` (the cluster that owns it; create a new folder if none fits).
5. **Types** — used app-wide → `types/character.ts`; used by one cluster only → that cluster's folder.

### `lib/` — pure-logic inventory

Source of truth: `ls frontend/src/lib`. No React/JSX; all unit-testable in isolation.

| File | Purpose |
|---|---|
| `dice.ts` | The sole `Math.random` dice site — `rollDie`/`rollSpec`/`summarizeRoll`/`formatRollSpec` (see Dice engine below). `RollSpec.crit` (#731) doubles the damage **dice** (`count`), never the modifier. Kept-die readers `keptD20`/`isNaturalTwenty`/`isNaturalOne` (#766) drive the attack sheet's nat-20 auto-crit + nat-1 miss — a nat 20 on the *dropped* die (disadvantage) is not a crit. |
| `rollMode.ts` | State-driven advantage/disadvantage resolver (#486). `resolveRollMode(rollModifiers, {kind, ability?}, manualMode)` → `{mode, sources}` (adv+disadv from different sources cancel to normal, RAW; the manual toggle overrides) + `rollModeChip(resolved)` for the "why" text. See Dice engine below. |
| `abilities.ts` | Ability/skill/save labels + `abilityModifier` math; resolve all display keys through here. `skillBonus` takes an optional `tempModifier` for active buffs. |
| `items.ts` | `isEquippable(category)` + `EQUIPPABLE_CATEGORIES` — equippability rule (weapon/armor yes, consumable/gear no). Mirror of backend `lib/inventory/items.ts`; gate the Equip control through here, never inline-check `category`. Also `itemCategoryLabel` + `ITEM_CATEGORY_LABELS`/`ITEM_CATEGORY_ORDER`/`ITEM_CATEGORY_OPTIONS` — resolve category display through here, never a raw key. |
| `events.ts` | Activity-log display lookups — `eventTypeLabel`/`categoryLabel`/`categoryTone` (tolerant `Partial<Record>` maps, raw-key fallback) + `INVENTORY_EVENT_TYPES` for the filter chips. Resolve all event type/category keys through here, never inline-capitalize. |
| `timeline.ts` | Groups/formats audit events for the activity timeline (`groupByBatch`/`groupByDate`, generic over `{id,batchId,createdAt}`). |
| `currency.ts` | Copper-based currency math — `toCopper`/`fromCopper`/`splitLumpSum` + `formatCurrency` (unsigned, largest-first denomination string). |
| `sellBatch.ts` | `summarizeSellBatch` collapses a bulk-sale batch (>1 row, all `sold`) into one line summary for ActivityModal; returns `null` for non-bulk-sale batches. |
| `toggleSet.ts` | `toggledSet<T>(set, key)` — returns a new Set with `key` toggled (added when absent, removed when present). Canonical immutable Set-toggle for multi-select state setters: `setSelection(prev => toggledSet(prev, id))`. Used by `ActivityModal` (field/batch expand state) and `useSellSelection`. |
| `bulkSell.ts` | Bulk-sell math. `buildSellOperations` (per-line `quantity` + `perItem`/`lumpSum` pricing); `defaultSellPrice` (half per-unit catalog value, rounded down, × qty); `resolveSellPrices` (single sale total + per-line pin overrides → per-line `Currency`, pinned lines exact + the rest split evenly, gp/sp/cp with **no** platinum roll-up); `gpToCopper`/`copperToGp` for the single decimal-gold input. Consumed by `SellPanel`/`InventoryList`; distinct from `sellBatch.ts` (which summarizes a completed batch). |
| `startingEquipment.ts` | Character-creation equipment helpers (`isPackageComplete`, `isGoldValid`, `EquipmentDraft`). |
| `paperDoll.ts` | Paper-doll slot taxonomy + placement rules for the Worn view (#566) — `allowedSlotsForItem` (mirrors the backend), `itemsInSlot`, `bagItemsForSlot`, `isOffHandLocked`, `equipSlotLabel`, `equippedLoadoutLabel` (the turn UI's "Longsword & Shield" / "Greatsword (two-handed)" / "Unarmed" hands summary, #733), `SLOT_GROUPS` (Hands/Armor/Adornment). Also the single source for the DM gear slot-authoring picker (#572): `WORN_SLOTS` (the eight worn slots, excluding the derived MAIN_HAND/OFF_HAND/BODY) + `wornSlotItemKindLabel`/`WORN_SLOT_ITEM_KIND_LABELS` (item-kind names — "Gloves" for HANDS, "Bracers" for WRISTS). Doll-polish helpers (#554), both reading data already on the wire: `isProficientWithItem(item, weaponProficiencies, armorProficiencies)` (frontend mirror of backend `srd.ts` `isProficientWithWeapon` — category grants match `weaponClass`, pluralised specific grants match the singular catalog name, armor grants match `armorCategory`; non-weapon/armor items never warn) and `versatileGrip(item)` (`{ short, full }` from the server-derived `weapon.damage.grip` snapshot — "1d10"/"1d10 · two-handed grip" when the off-hand is free, "1d8" when it's filled; null for non-versatile weapons). |
| `loadout.ts` | JSX-free model for the Worn `LoadoutList` (#925): `buildLoadoutGroups(character)` walks the `paperDoll.ts` slot taxonomy into ordered **Weapons / Armor / Accessories** groups of `filled`/`empty`/`locked` `LoadoutRow`s (RING expands to `RING_CAPACITY` rows; a two-handed main-hand locks the off-hand), carrying the derived `notProficient` (`isProficientWithItem`) + `grip` (`versatileGrip`, main-hand only); `LOADOUT_GROUP_LABELS` renames the doll's slot groups; `attunementSummary(inventory)` → `{ count, cap, atCap }` for the Attuned N/3 header (`ATTUNEMENT_CAP` = 3). |
| `loadoutPicker.ts` | JSX-free core of `InlineLoadoutPicker`'s per-hand picker (#789, re-homed into the Action sheet #815): `handContext` (live MAIN/OFF occupants + `actionsRemaining`), `handPickerOptions` (bag candidates deduped by name with ×N counts + a free Stow, each tagged `costsAction`/`disabledReason`), `handButtonDisabledReason`. Mirrors `useLoadoutSwap`'s `buildSwapOps` cost rule — a swap costs the Action when it stows a held item (the target hand, or the other hand for a two-handed weapon). Gating is per-option/per-hand only (the row-level `noLegalMove` gate was dropped with the loadout row in #815); free-hand draws stay reachable at 0 actions because the spent Action slot renders a free-move "Change weapons" button (`ActionSlot`) that opens the same picker. Keeps the component under fallow thresholds. |
| `campaignItemForm.ts` | JSX-free form↔input mapping for the DM item authoring form (`CampaignItemForm`) — `FormState`/`emptyForm`, `formFromItem`/`formFromCatalog` (prefill), `buildInput` (form → `CampaignItemInput`, with the magic/versatile/range/slot gates), `weaponFields`/`currencyFromForm`/`unitForCost`/`hasRange` + option constants. Keep the gating rules here, never re-derive them in a component. |
| `characterCreationValidation.ts` | Explains *why* the creation Save button is disabled (`missingRequirements`). |
| `abilityGen.ts` | Ability-score generation methods (point-buy / standard array / roll). |
| `dieFaces.ts` | Static die-face geometry data for the 3D rollers. |
| `physicsDice.ts` | Physics-roller setup (cannon/three glue) for `PhysicsDiceRoller`. |
| `effects.ts` | Mirror of backend `lib/combat/effects.ts` (keep in sync) — the 5e effect model (dice + save + scaling). `readEffectSpec(row)` adapts the flat effect columns into an `EffectSpec`; `resolveEffectSpec(spec, effectiveStep, ctx)` returns a concrete `RollSpec`, generalizing the scaling axis (`cantripLevel`/`slotUpcast`/`ki`). Includes the `buff` EffectType mirrored from the backend. `spellCast.ts` + `spellMeta.ts` both delegate here — never re-copy the scaling math. |
| `spellCast.ts` | Pure cast-roll math shared by SpellsSection + InlineSpellPicker. `computeCastSpec` derives the spellcasting ability mod then delegates the scaling/heal math to `resolveEffectSpec` (`lib/effects.ts`). `planCast(spell, character, slotLevel?)` returns a `CastPlan` (`{ ops, result }`) — the spellcasting ops to send plus the `CastResult` banner to show — rolling internally via the now-private `computeCastRoll`; `SpellsSection`'s `useSpellcasting` hook wires it. |
| `spellMeta.ts` | Pure spell display helpers (school tone, metadata, `defaultTarget`/`targetLocked`, `effectPreview`/`effectPreviewWithMod`, `isAttackCantrip` — `level 0 && attackType "attack"`, the Fire-Bolt filter routing attack cantrips through the attack sheet, #734) shared across spell surfaces. The effect-preview count/modifier come from `resolveEffectSpec` (`lib/effects.ts`); this file only formats the label. `Target` is `"self" \| "other" \| AllyOption`; `partyHealAllies(session, selfId)` derives the opted-in ally list a healing cast can target — present participants sharing the campaign with `autoFriendlyHealing` on (#462). `SpellTargetToggle` renders those allies (vs. self/other for damage) and `useSpellPicker` sends the pick as `apply.target: { characterId }`. |
| `spellPicker.ts` | Pure InlineSpellPicker selection/slot predicates (`availableSlotLevels`, `availableSlotsForSpell`, `resolvedSlot`, `filterCastableSpells`, `sortSpells`, `spellRestrictionFlags`, `slotRestrictionHint`) plus the level-section presentation helpers (`groupSpellsByLevel`, `hiddenSpellLevels`/`hiddenLevelsNote` — the "Level 2+ hidden — no slots remaining" footer that makes the slot-gating filter visible, `slotPipsForLevel`, `castCostBadge`). The slot predicates (`availableSlotLevels`, `availableArcanaLevels`, `availableSlotsForSpell`) are the single source for both the session picker and SpellsSection — `spellList.ts` imports them from here. |
| `spellList.ts` | Pure SpellsSection list derivation (#606). `deriveSpellList(character)` returns a `SpellListDerivation` (available slot/arcanum levels, learned catalog spell ids, level-sorted spells + present levels, dismissible while-active spell buffs, single-class-warlock Pact-Magic labeling). Sibling of `spellPicker.ts` (the session-mode equivalent), from which it imports the shared slot predicates — this file backs the out-of-session sheet section. |
| `spellRow.ts` | Pure `SpellRow` derivations (#607). `deriveSpellRow(spell, availableSlots)` returns castability/provenance (`isCantrip`, item/atWill/chargeCost/itemExhausted, `isGranted`, `schoolTone`, `noBudget`); `resolveCastAction(spell, availableSlots)` routes a Cast click (`cast` / `castAt` / `openPicker`); `upcastSlotOptions(spell, characterLevel, availableSlots)` builds the per-slot picker rows (upcast flag + scaled `effectPreview`). |
| `addSpell.ts` | Pure `AddSpellPanel` logic (#607): `filterCatalog(catalog, search, levelFilter)`, `buildCustomSpellPayload(custom, hasEffect)` (effect fields ride along only when opted in), `catalogMetaLine`/`catalogEffectLine` result-row label formatters, plus the `LEVEL_OPTIONS`/`SPELL_SCHOOLS`/`BLANK_CUSTOM` constants and shared `INPUT_CLS`/`LABEL_CLS` input classes. |
| `turnRules.ts` | 5e turn economy — universal action lists (Grapple and Shove are separate entries, both `simple-confirm`) + `canTwoWeaponFight(inventory, fightingStyle?)` (two equipped light weapons → TWF; the **Two-Weapon Fighting style** relaxes the light restriction, #732). (Extra Attack counts are server-derived; read `character.attacksPerAction`.) |
| `turnOptions.ts` | Pure render models for the option-card picker sheets: `mainWeaponSummary`/`offHandSummary` ("Longsword · +7 to hit · 1d8 + 4 slashing", via `attackMath`), `consumableCount`, `classActionOption` (+`poolBadgeFor` — resolver-derived heal subtitle + "1 / rest" pool badge), `bonusSpellOptions` (castable bonus-action spells as cards; filtering argument-identical to `useSpellPicker`), `twfHint` (named light-weapon pair suggestion), `PRIMARY_ACTION_KEYS`/`MICRO_CAPTIONS`/`moreActionsPreview` (More-actions disclosure), and the `ActionSheetModel`/`BonusSheetModel`/`ReactionSheetModel` interfaces built in `useTurnActions`. |
| `turnActionPlan.ts` | Pure planner for TurnHub action-slot clicks (#613): `planActionClick(resolver, character)` → an `ActionClickPlan` (`{ consumeSlot, openResolution, send: "none"\|"plain"\|"healRoll", healRoll? }`) that `useTurnActions.handleActionClick` interprets. Keeps the per-kind branching testable and out of the hook's dispatch closure. All picker-opening kinds (`spell-picker`, and since #765 `item-picker` + `heal-input`) plan `consumeSlot: false` — opening the sheet is free; the slot commits via the sheet's `onCommit` callback when the player actually uses/casts/heals (see the deferred-commit note in the SessionPage section). |
| `sessionTabs.ts` | Pure SessionPage reference-tab construction (#613): `buildSessionTabs({ isCaster, hasClass, isOwner })` (conditional Spells/Class/Loot inclusion) + `resolveActiveTab(tabs, activeTab)` (fall back to Inventory when the active tab was gated away). Feeds `SessionReferenceTabs`. |
| `attackMath.ts` | Pure attack-row math for InlineAttackPicker: `buildAttackForms` (#786 — the ordered "Attacking with" form list the single attack card renders from: deduped equipped weapons + Unarmed + Improvised, first row = main-hand default), `buildAttackEntries` (equipped/unarmed/improvised rows, un-deduped + precomputed roll/log label strings), `buildEquippedWeaponEntries` (#756 — equipped-weapon rows deduped by name), `buildUnarmedEntry`/`buildImprovisedEntry` (the Unarmed Strike / Improvised Weapon forms — selector segments feeding the one attack card, not standalone rows since #786), `buildOffHandEntry` (#732 — the single OFF_HAND weapon row for the Two-Weapon Fighting bonus attack; drops `damage.abilityModifier` from off-hand damage unless the character has the `twoWeaponFighting` style, keeping a negative mod and any melee buff), grip-resolved weapon damage/type/grip helpers, `critDamageSpec` (#731, the crit-hit doubler — see Dice engine), unarmed display, `hasSuperiorityDice`, `attacksExhausted`. **Dice-valued item damage riders (#547):** `weaponDamageRiders(item)` collects an active item's dice-valued `add`-op `damage` passiveBonus caps as typed `DamageRider`s (each keeps its own damage type; a `condition` becomes self-announce reminder text), rolled as separate damage terms at attack time; `capabilitiesActive(item)` is the frontend activation gate (attunement-required item needs attunement, others equip — mirrors backend `isItemActive`). |
| `attackResult.ts` | Pure `AttackResultLine` derivation (#778): `resultLineView(result, kind, overrideTotal)` returns everything the row renders — kept dice + faces, effective total (maneuver override wins), `critHit`/`miss` cues (to-hit nat-20/nat-1 only), `critSpec` (doubled-dice roll), and the arcane/garnet `tone` — so the component is render-only. |
| `attackTallySummary.ts` | Pure model + formatting for the multi-attack tally (#802/#811/#813): the `AttackTallyRow` / `TallyAttackRoll` / `TallyVerdict` / `TallyRowSource` types (owned here, re-exported from `useTurnState`). Each row carries a stable `id` (damage/rider/override writes target it by id, never "the last row") and a `source` (`action` \| `bonusAction`) so the Attack-action and off-hand-TWF swings share one tally without misattributing. `autoVerdict` (nat 20 → crit, nat 1 → miss, else undefined-manual), the `isVerdictLocked`/`isMissRow`/`isCritRow`/`isUnresolvedRow` predicates, and `attackTallyLine`/`attackTallyLines` for the "Turn summary" banner lines — an unresolved row reads "to-hit N — hit or miss?", never "hit"; off-hand rows carry the "Dagger (off-hand)" `formName`. Rendered by `AttackTallyStrip` (in-sheet, filterable by `source`) + `TurnSummaryBanner` (turn card); rows recorded by `useTurnState`. |
| `attackStepRail.ts` | Pure step model for the attack sheet's 1-2-3 rail (#811): `stepRail({hasRoll, verdict, hasDamage})` → done/active/pending per step. Damage arms as soon as a roll exists (implicit hit — no verdict gate); a miss verdict parks it. Rendered by `AttackStepCard`. |
| `mentions.ts` | @-tagging primitives (#248/#269): `parseMentionBody` (text/mention segment split of a stored body), `normalizeForMatch` (search key, parity with backend `lib/activity/journal-refs.ts`), `matchEntities` (name/alias only — the @-autocomplete matcher, must never match notes), `matchEntitiesDetailed` (#840 — codex search variant that also matches notes text, flagging `matchedInNotesOnly` so the ledger can mark it secondary), `parseTrigger` (the in-progress `@…`/`@type:` autocomplete trigger). Per-type display maps: `ENTITY_TYPE_LABELS`/`ENTITY_TYPE_OPTIONS`/`ENTITY_TYPE_TONE` plus the Tailwind class maps `ENTITY_TYPE_DOT_CLASS` (filter tone dots), `ENTITY_TYPE_MONOGRAM_CLASS` (ledger monogram tiles), `MENTION_CHIP_TONE_CLASS` (edit-time composer chip tints, #842), and the **inked-name** mention maps (#862) `ENTITY_TYPE_INK_TEXT_CLASS` (`text-*-800` ink per type — garnet for NPC+PC, vitality for LOCATION, gold for ITEM, arcane for FACTION, parchment for OTHER) + `ENTITY_TYPE_INK_BORDER_CLASS` (the same ink at `/45` for the dotted underline), composed with the font-agnostic `MENTION_INK_BASE_CLASS` recipe (`border-b border-dotted font-semibold [font-variant-caps:small-caps]`). The `-800` step is intentional — the reversed dark ramp makes it light-on-dark, so both themes clear WCAG AA as text. Add new per-type class maps here, never inline in a component. Edit-time DOM helpers (contenteditable composer): `mentionBodyToFragment` (body→DOM with chips), `serializeMentionDom` (DOM→body round-trip), `serializeMentionDomBeforeCaret` (pre-caret slice for trigger parsing), `placeCaretAtBodyOffset`, `spliceMentionToken` (splice `@[<uuid>] ` into a body at a trigger range, returning the new body + caret offset), `resolveAdjacentChip` (find the chip adjacent to a collapsed caret for Backspace/Delete removal), plus the `MentionResolved` / `MentionSegment` / `MentionTrigger` types. Pure — no JSX. |
| `codexLedger.ts` | Pure codex-browse helpers (#840): `groupByInitial` (A→Z `LetterGroup[]`, normalized initials so "Élise" files under E, non-alphabetic under a trailing `#` bucket), `typeCounts` (zero-filled per-type tallies for the rail), `notesSnippet` (first non-empty line for the row snippet), `monogram`, `CodexSort`/`CODEX_SORT_OPTIONS`, the mention-sort helpers (#853): `compareByRecentMention` (latest `lastMentioned` date desc, session-ordinal then normalized-name tiebreaks, never-mentioned/statless last), `compareByMentionCount` (count desc, statless as zero, normalized-name tiebreak), `mergeEntityStats` (attach stats to plain-list rows by id) and `buildLedgerGroups` (letter groups for alpha, one ranked pseudo-group for mention sorts), and the activity-rail derivations (#841): `needsChronicling` (mentioned + descriptionless `StatsEntity[]`, most-mentioned first) and `mostMentioned` (top-n leaderboard via `compareByMentionCount`, zero-mention drop). |
| `entityBacklinks.ts` | Pure chronicle grouping for the entity article (#842): `chronicleGroups` (session-primary `ChronicleGroup[]` — key/sessionId/title/ordinal, group date from its newest item, null-session entries in a trailing-order "none" group, API newest-first order preserved) and `splitChronicle` (latest-N visible/hidden split behind the "Show earlier sessions" expander, default 3). |
| `encumbrance.ts` | Carrying capacity (`carryingCapacity` = STR × 15) and coin weight (`coinWeight(currency)` = total coins ÷ 50 lb, PHB p.143), both derive-on-read. |
| `inventorySections.ts` | JSX-free Bag-view derivations for `InventoryList` — `filterInventory` (filter chip + name search), `buildSections` (canonical category order, equipped-first then alphabetical, empty sections dropped), `itemsWeight`, `selectionGp` (multi-select sell estimate), `formatWeight`, and the `FilterKey`/`FILTERS` chip list. Keep the filter/section rules here, never re-derive them in a component. |
| `itemDetails.ts` | Pure inventory-row presentation: `itemDetailParts` (the dotted summary line), `hasItemProse`. Shared by InventoryRow/ItemSummary. |
| `itemCard.ts` | Pure detail-row derivation for the Codex item card (#687) — `diceLabel` (dice-notation formatter) and `itemDetailRows(item)` (ordered `{label, value}[]`: weight, value, then category detail). No JSX; `CampaignItemCard` maps these rows to its `DetailRow` primitive. |
| `fightingStyles.ts` | Fighting-style labels/descriptions (presentation; backend is rules source of truth). |
| `damageTypes.ts` | The standard 5e `DAMAGE_TYPES` list + `damageTypeLabel` (populate the HP damage-type picker) and `activeResistedDamageTypes(buffs)` — the self-scoped resistance registry that drives the auto-halve preview (#456). Mirrors the backend helper of the same name. |
| `multiclass.ts` | Multiclass display + gating helpers: `isMulticlass`, `classSummary` (single-class → name unchanged; multiclass → "Wizard 5 / Cleric 3"), `multiclassPrereqMet` (evaluates the backend-served `ClassOption.multiclassPrerequisite` thresholds against the character's scores — no rules table duplicated). Feeds `CharacterSheetHeader`/`CharacterCard`/`ClassFeaturesSection`, `AddClassPanel`, `LevelUpModal`. |
| `disciplines.ts` | Four Elements ki rules (mirror of backend): `maxKiPerDiscipline` cap, base-cost/scaling reads, `disciplineKiOptions` selector range, `disciplineRollSpec` (ki-scaled effect roll), plus the row-view derivations (#688): `disciplineCastView` (base/options/affordability/ki chip label), `effectiveKiSelection`, `disciplineRollLabel`, `disciplineCastTitle`. Feeds `DisciplineRow`/`DisciplinesSection`. |
| `shadowArts.ts` | Pure Way of Shadow row derivations (#688): `shadowArtView(art, ki, isConcentrating, concentratingOnName)` → prefix-stripped display name, ki cost/affordability, concentration flags, buff chip label (via `skillLabel`), and the will-replace-concentration warning. Feeds `ShadowArtRow`. |
| `maneuverPrompt.ts` | Pure planning for the weapon-row maneuver prompt (#689): `maneuverPlacement` (legacy default `damageRoll`), `planManeuverPrompt` (attackRoll/damageRoll routing + section visibility — `attackOption`/`reaction`/`effect` maneuvers never reach a weapon row), `resolveDamageSelection` (stale-selection fallback), `canPromptManeuvers` (render gate). Feeds `ManeuverPrompt`, which takes a `section` prop (`"attack"`\|`"damage"`) so the attack card and the damage card each mount only their own half (#809). |
| `conditions.ts` | 5e condition labels/descriptions for the chip strip + picker. |
| `rarity.ts` | Item-rarity presentation (mirror of backend `srd.ts` `ITEM_RARITIES`, #497): `ITEM_RARITY_LABELS`/`rarityLabel` (resolve display through here, never a raw `VERY_RARE` key), `RARITY_OPTIONS` (dropdown, ascending tiers), `rarityTone` (Badge tone per tier), and `standardValueForRarity`/`rarityValueHint` (derived gp hint — consumable halves, Artifact priceless). Feeds `CampaignItemsPanel` + `CampaignItemCard`. |
| `capabilities.ts` | Item-capability presentation (mirror of backend `lib/inventory/capabilities.ts`, #546): `CAPABILITY_TARGET_OPTIONS`/`CAPABILITY_OP_OPTIONS`/`ATTUNEMENT_PREREQ_OPTIONS`/`CHARGE_TRIGGER_OPTIONS` (authoring pickers), `targetUsesSkillKey`/`targetUsesAbilityKey` (which targets name a skill/ability via `targetKey`), `capabilitySummary` (one-line bonus, e.g. "+2 Stealth" / "+2d6 fire Damage (when on hit)" — resolves `targetKey` through `skillLabel`/`abilityLabel`, never a raw key; a charges pool renders via the internal `chargesSummary`: "7 charges · regains 1d6+1 at dawn", #555), a cost-aware `castSpellSummary` ("costs 3 charges" when `resource === "charges"`), and `describeAttunementPrereq`. Feeds `CapabilityEditor`, `AttuneToggle`, `ItemSummary`. |
| `capabilityDraft.ts` | JSX-free capability-draft logic for the `CapabilityEditor` subcomponents (#604): the `NEW_*` kind defaults + `draftForKind` (fresh draft on a kind switch), `keyOptions` (skill/ability key list for a target), and the field-normalization patch builders `applySpell` (clears the DC/attack the picked spell doesn't use), `applyTarget` (resets a stale `targetKey`), `applyDiceToggle`, `applyGrantType` / `applyProfKind` / `applyAdvantageOn` (reset dependent fields on a grant-axis change). Keep this normalization here, never re-derive it in a field component. |
| `activatedEffect.ts` | Display labels for an `activatedEffect` capability's activation type — `activationLabel`/`ACTIVATION_LABELS` (action/bonus/reaction/commandWord), mirroring the backend `describeActivation`. Feeds `ActivateControl`. |
| `characterSections.ts` | Sheet-section visibility predicates (`hasProficiencies`/`hasAdvancements`) — the inline card-gate expressions from the sheet panels (OverviewPanel). |
| `overviewSkills.ts` | `proficientSkills(skills)` — the proficient/expertise-only, label-sorted skill list for the Overview `ProficientSkillsCard` summary (#923). |
| `formatJournalDate.ts` | Formats ISO journal dates in UTC ("Jun 22, 2026"); `formatRelativeDay` gives "today"/"yesterday"/"N days ago" by UTC calendar-day diff, absolute past 30 days (#841). |
| `sessionRecap.ts` | Pure recap-shaping for `SessionSummaryModal` — `formatDuration`/`formatTimeRange` (window strings), `sortSlotsSpent` (drop-zero + ascending slot pairs), `withSummary` (participants carrying a computed summary) + the `SummarizedParticipant` type. |
| `advancement.ts` | `entryDetail` — pretty-prints an AdvancementEntry's ASI/feat effects for AdvancementSection's list view. |

### `@/` path alias

`@/` maps to `frontend/src/`. Configured in `tsconfig.json` (`paths`) and `vite.config.ts` (`resolve.alias`); Vitest inherits it automatically from Vite.

```ts
// ✓ Always use @/ for cross-file imports
import Badge from "@/components/ui/Badge";
import { useCharacter } from "@/hooks/useCharacter";
import type { InventoryItem } from "@/types/character";

// ✗ Never use relative ../ paths — they break on moves and are hard to grep
import Badge from "../../components/ui/Badge";
```

Use `@/...` for **every** source import — including same-folder siblings — so paths survive component moves and remain grep-able. The only exceptions are asset side-effect imports in `main.tsx` (e.g. `import "./index.css"`).

## Tailwind v4

**Setup**: loaded via `@tailwindcss/vite` in `vite.config.ts`. No `tailwind.config.js` or `postcss.config.js` — this is correct v4 practice; do not add them. The only Tailwind setup is `@import "tailwindcss";` in `frontend/src/index.css`.

**Named utilities work normally.** Named size utilities (`max-w-xl`, `max-w-6xl`, `w-96`, `h-24`, etc.) and numeric spacing (`p-4`, `gap-2`, `w-14`) all resolve correctly in Tailwind 4.3.1.

**Prefer idiomatic utilities over verbose arbitrary values.** Custom `@theme` tokens auto-generate idiomatic Tailwind classes — use these:

```tsx
// ✓ Idiomatic (preferred)
<div className="text-garnet-700 bg-parchment-50 rounded-card shadow-card">

// ✗ Verbose (legacy, avoid for new code)
<div className="text-[var(--color-garnet-700)] bg-[var(--color-parchment-50)] rounded-[var(--radius-card)] shadow-[var(--shadow-card)]">
```

Genuine non-token values still use arbitrary syntax: `text-[11px]`, `max-h-[80vh]`, `max-w-[36rem]`.

**Footgun:** never reintroduce bare `--spacing-{name}` tokens — see the Tailwind non-negotiable in CLAUDE.md.

## Design tokens

Token definitions live in `frontend/src/index.css` (`@theme` block). For a full reference of color families, typography, spacing, radius, and shadow scales, see:

**`.claude/agent-memory/frontend-design-architect/design_system.md`**

Summary of what's available:
- Colors: `--color-parchment-{50..900}`, `--color-garnet-{50..900}`, `--color-arcane-{50..900}`, `--color-gold-{50..900}`, `--color-vitality-{50..900}`
- Fonts: `--font-display` (Source Serif 4, headings), `--font-sans` (Source Sans 3, body/UI)
- Radius: `--radius-card` (0.625rem), `--radius-control` (0.375rem) — just two, used everywhere
- Shadows: `--shadow-card`, `--shadow-raised`
- Texture (#228): `--texture-grain` (inline-SVG fractal-noise data-URI) plus per-theme `--texture-blend`/`--texture-page-opacity`/`--texture-card-opacity`. A fixed `body::before` paints the page-canvas grain; the `.surface-grain` class (on `Card`) paints a faint `::after` grain clipped to the card radius. Both are `pointer-events:none`. See `design_system.md`.

Use idiomatic utility classes — tokens auto-generate them in v4: `text-garnet-700`, `bg-arcane-50`, `rounded-card`, `shadow-raised`. Only fall back to `[var(...)]` syntax for non-token one-off values.

**Text-contrast policy (WCAG AA).** On the `parchment-50` background nothing lighter than `parchment-600` clears 4.5:1, so **readable text uses `parchment-600` or darker** (`-600` secondary, `-700`/`-900` primary). `text-parchment-400`/`-500` are **reserved for WCAG-exempt uses only** — `placeholder:`, `disabled:`, and decorative `aria-hidden` glyphs — never for content text. Don't reintroduce `-400`/`-500` on readable text (see #158/#98). The same 4.5:1 floor applies to **accent text on light surfaces**: `gold` ≥ 800, `arcane` ≥ 700, `garnet` ≥ 600 (bump one step darker on a tinted fill, e.g. `arcane-800` on `bg-arcane-100`) — the mid steps (`gold-700`, `arcane-600`, `garnet-500`) fail as text. Lighter accent steps are for fills/borders/meters, not text (see #187/#207). The mirror rule applies to **light text on an accent fill** — it must also clear 4.5:1, so only the darker accents can carry white: `garnet-600` (≈5.5:1) and `vitality-600` (≈4.9:1) do; `arcane` carries white only from `arcane-700` down (`arcane-600` ≈3.8:1, `arcane-700` ≈5.07:1, hover `arcane-800` ≈6.44:1); and **gold can never carry white** (`gold-800` technically passes at ~6:1 but reads muddy), so filled gold flips to **dark text on a bright fill** on `bg-gold-400` (hover `bg-gold-500`) (see #207). **Dark mode (#211/#213): a label token on an accent fill must co-flip with its fill or stay fixed.** Garnet/arcane/vitality fills **invert** between modes, so their labels use **`text-parchment-50`** (near-white in light, near-black in dark) — never hard-coded `text-white`. Gold is light-ish in **both** modes, so its label uses **`text-ink`** (the fixed `--color-ink` #27241d that never flips), ≈10.5:1 light / ≈5.6:1 dark — never `text-parchment-900` (which flips light in dark and fails AA). Full rationale in `design_system.md`.

### Design gate

Staying on-system is what keeps the UI from reading as generic. The `verify-frontend` skill runs a **design-review lane** (the `frontend-design-architect` agent — or a `general-purpose` agent briefed with the design docs if that type isn't available — plus the `/ux-review` skill for whole-page changes when it's installed) alongside unit tests and browser verification. It judges changes against this token set and the conventions in this doc — off-token colors/radii/shadows, broken hierarchy, reinvented primitives, and raw skill/ability keys are `blocking` findings that fail the gate; subjective polish is `advisory`. Run `/verify-frontend` before opening a frontend PR (it's also invoked automatically by `/parallel-issues`).

## UI pattern: inline panels vs Modal overlay

**Rule**: `Modal.tsx` (portal + focus trap + Esc + backdrop) is reserved for **read-only review surfaces and confirm dialogs**. Every add/edit surface is an **inline expand-in-place panel within its Card**.

| Modal | Inline panel |
|---|---|
| `ActivityModal` — filterable audit timeline (category + session selects + inventory type chips; optional `entityId` scope) + undo — also serves as inventory history | `AddItemPanel` — add item form |
| `DeleteCharacterModal` — confirm destructive action | `AddSpellPanel` — learn spell form |
| `LevelUpModal` / `ConcentrationSaveModal` — hosted *inside* `HitPointTracker` | `InventoryRow` edit mode |
| `DiceRollModal` — read-only 3D roll result, hosted *inside* `RollProvider` (skill/ability/save/initiative rolls) | |
| | `SellPanel` — bulk-sale confirm/review (per-line qty + one sale total that splits evenly; optional per-line price override) |
| | `HitPointTracker` itself — inline Card (damage/heal/rest/death-save controls) |
| | `ExperienceTracker` award/set inputs |
| | `AbilityScoreEditor` method tabs |

When adding a new editing surface: **default to inline**. Reach for `Modal` only if the surface is read-only or a destructive confirmation. If you need an overlay for an editing surface, make the case explicitly.

**Mobile turn UI exception — `BottomSheet` (#729, responsive #747).** The in-session turn economy (`features/session/`) is a mobile-first, thumb-driven surface: the Action/Bonus/Reaction slots are large tap targets that open a `BottomSheet` picker ("nothing is spent until you choose"), and the attack/spell/item/heal resolution pickers render in a sheet too. `BottomSheet` is responsive — a bottom-anchored slide-up sheet on mobile, a centered modal at `md`+ (#747) — so it reads well on desktop too. `BottomSheet` and `Modal` share their focus-trap/Escape/scroll-lock machinery via `hooks/useDialogChrome.ts` — build any new overlay on that hook rather than re-hand-rolling the `keydown`/scroll-lock/focus-restore block. Keep `BottomSheet` scoped to the turn surface; elsewhere the inline-vs-Modal rule above still holds.

## Primitive components

These live in `src/components/ui/` and are intentionally domain-agnostic — they must not import from `@/features`, `@/api`, or `@/types/character`. They know nothing about D&D.

`OverflowMenu`, `DropdownMenu`, and `Popover` all share their Esc-to-close + click-outside-to-close behavior via `hooks/useDismissable.ts` — add a new dismissable popup on top of that hook rather than re-hand-rolling the `keydown`/`mousedown` listener pair. (The entity hover preview, `features/entities/EntityPreviewCard`, is deliberately **not** a `Popover`: it's hover-intent-triggered, non-focusable/`aria-hidden`, and portals to `document.body` — no trigger ownership, dialog role, or focus behavior to share.)

| Component | Usage |
|---|---|
| `Card` | Base parchment surface for every major section. Props: `title?`, `titleAccessory?`, `className?`, `headingLevel?` (`2`\|`3`, default `3` — set `2` when the card is a top-level page section directly under the page's `h1` so heading order doesn't skip). |
| `Badge` | Soft-background pill. Prop `tone`: `garnet` / `arcane` / `gold` / `vitality` / `neutral`. |
| `MeterBar` | Horizontal resource meter. Always pair with numeric text (e.g. `9/10 HP`) — never rely on color alone. Prop `tone`: `garnet` / `arcane` / `gold`. |
| `Modal` | Centered overlay primitive. See inline-vs-modal rule above. Shares focus/scroll machinery with `BottomSheet` via `hooks/useDialogChrome`. |
| `BottomSheet` | Responsive picker for the turn UI (#729): a bottom-anchored slide-up sheet with a grabber handle on mobile, a centered dialog with all corners rounded and no grabber at `md`+ (#747). Same parchment/`--shadow-raised` DNA and `useDialogChrome` behavior as `Modal`. Dismissal per breakpoint (#767): **mobile** — drag the sheet down past ~1/3 its height or flick to close (`hooks/useDragToDismiss`; the content region only follows the finger when scrolled to top and dragged downward, else it scrolls), the grabber is a real `<button aria-label="Close">`, plus scrim-tap + Escape; the text Close button is hidden. **`md`+** — drag is inert and the text Close button returns; scrim-tap + Escape unchanged. **Dismissal animates out on mobile (#782):** every mobile close path — drag, grabber tap, scrim tap, Escape — routes through `useDragToDismiss`'s `beginExit`, which slides the panel off the bottom edge (`translateY(100%)`, 500ms iOS curve `cubic-bezier(0.32,0.72,0,1)`) and fires `onClose` on the panel's `transitionend` (with a timeout fallback, guarded to fire once); the scrim fades to `opacity-0` on a matching 500ms transition, and pointer input is inert mid-exit. A short drag still springs back (220ms, #775 cancel-always-springs-back untouched). At `md`+ the centered dialog keeps today's instant close. Honors `prefers-reduced-motion` (no follow/spring/slide-out, dismisses immediately). The mobile/`md`+ split reads `hooks/useIsBelowMd.ts` (shared with `CapturePalette`'s per-breakpoint presentation, #771). **Keyboard-aware height (#784):** on mobile the panel's `max-height` is capped to `min(85vh, ${visible-viewport}px)` via `hooks/useVisualViewportHeight.ts` (tracks `window.visualViewport`, `innerHeight` fallback) so when the on-screen keyboard opens for an auto-focused input the body sits above it rather than behind it; no cap at `md`+. `useDialogChrome` focuses the panel with `{ preventScroll: true }` so iOS doesn't reveal-scroll the fixed sheet on open. Props: `title`, `subtitle?`, `onClose`, `children`. Used by the turn surface and the Quick-capture palette. |
| `Tabs` | Controlled segmented-control tab switcher (WAI-ARIA tablist, arrow-key nav, optional per-tab `badge`). Renders only the switcher; the caller renders the active panel below it. Props: `tabs`, `active`, `onChange`. |
| `OverflowMenu` | Icon-only kebab (`MoreVertical`) menu-button (WAI-ARIA menu-button: `aria-haspopup`, roving tabindex, Arrow/Home/End/Esc nav, click-outside to close, focus returns to trigger). No portal — `relative`-anchored popup. Per-item `danger?` (garnet) / `separatorBefore?` (divider). Props: `items`, `label?` (trigger accessible name, default "More actions"), `className?`. |
| `DropdownMenu` | Owned-trigger popup menu for **arbitrary** content (vs `OverflowMenu`'s fixed item array). Owns the `<button>` and takes the trigger as `trigger` content; `children` is a render-prop `(close) => ReactNode`. Keyboard nav (Arrow/Home/End + roving tabindex) is driven by a **live** `[role^="menuitem"]` DOM query (covers `menuitemradio`/`menuitemcheckbox`), so presentational rows carry no role and are skipped for free. `aria-haspopup`/`aria-expanded`, ArrowDown/Enter/Space opens, Esc + click-outside close, focus returns to trigger. No portal — `relative`-anchored. Props: `trigger`, `label` (trigger accessible name), `children`, `align?` (`right`\|`left`, default `right`), `className?`. |
| `Popover` | Owned-trigger **disclosure** popover for read-only detail panels (vs `DropdownMenu`'s menu semantics — no menuitems, no roving focus). Trigger gets `aria-haspopup="dialog"`/`aria-expanded`; panel is `role="dialog"` (focused on open), Esc closes and refocuses the trigger, click-outside and re-click close. No portal — `relative`-anchored. Props: `trigger`, `label` (trigger + dialog accessible name), `children` (a `ReactNode`, or a render fn `(close) => ReactNode` so panel controls can dismiss the popover), `align?` (`left`\|`right`, default `left` — the *preferred* side: on open, a `useLayoutEffect` measures the anchor + panel against `documentElement.clientWidth` and auto-flips to the opposite side if the preferred one would overflow the viewport horizontally, e.g. a right-column paper-doll slot on mobile; recomputed on window resize while open), `className?`, `triggerClassName?` (style the owned button, e.g. as a stat tile), `id?` (DOM id on the trigger button, so another control can move focus to it — e.g. the paper-doll off-hand lock focusing its two-handed main-hand owner, #554), `onClose?` (fires on every open→closed transition). |
| `Avatar` | Circular identity badge. Renders `<img alt="">` when `imageUrl` is set, else initials (up to two from `name`, then the `email` initial, then `?`). Decorative — the accessible label lives on the trigger. Props: `name`, `email`, `imageUrl` (all primitive, nullable), `className?` (default `h-8 w-8`). |
| `ErrorBoundary` | Class error boundary wrapping the route tree in `App.tsx`. Catches render-time crashes and shows a parchment "something went wrong" fallback (Reload / Back to characters) instead of a blank page. Optional `fallback?: (error, reset) => ReactNode` for custom recovery UI. |
| `EmptyState` | Warm zero-state: decorative hero icon (pass a game-icon from `icons.ts`) + `font-display` title + optional `description` and `action` CTA. Prop `size`: `md` (card-body, default) / `sm` (in-card strip). Used for empty journal / inventory / spellbook / conditions. |
| `Spinner` | Loading indicator (`role="status"` + visually-hidden "Loading…"). Prop `variant`: `page` (larger, centered in a full-screen `min-h-screen` container) / `inline` (small, centered in its block, default). **Always gate it with `useDelayedFlag`** (see Loading pattern below) so it only appears for genuinely slow loads — fast loads must render nothing, never a flashing spinner or text. |

### Form primitives (#542)

Token-styled, controlled building blocks for authoring forms. `Input`/`Textarea`/`Select` share `controlClass` (exported from `Input`) so every control surface matches; numeric inputs keep an explicit `text-parchment-900` for dark-mode legibility.

| Component | Usage |
|---|---|
| `Field` | Label + control + hint/error wrapper. Props: `label`, `htmlFor?`, `hint?`, `error?` (error takes precedence over hint), `required?`. Wrap any control to get consistent label + helper-text layout. |
| `Input` / `Textarea` / `Select` | `forwardRef` wrappers over the native element, pre-styled with the shared `controlClass` (Textarea adds `resize-y`). Spread all native attributes. Reach for these instead of hand-rolling `className={inputCls}`. |
| `Segmented` | Single-select segmented control (WAI-ARIA `radiogroup`, roving tabindex, Arrow/Home/End nav), styled like `Tabs`. Generic over the value union. Props: `options` (`{value,label}[]`), `value`, `onChange`, `label`. Use for a small, mutually-exclusive choice (category, weapon class) instead of a `Select`. |
| `ChipToggle` / `ChipGroup` | `aria-pressed` pill toggle + a labelled wrapping `role="group"` row. Use for a set of independent booleans (weapon/armor property flags) instead of a stack of checkboxes. |
| `Disclosure` | Collapsible section: a disclosure button (`aria-expanded`/`aria-controls`) revealing a region. Use to tuck advanced/rarely-touched fields (e.g. the coin breakdown) behind progressive disclosure. |
| `DiceInput` | Compound `NdF (+M) [type]` dice control (own `DiceValue` type). Props: `value`, `onChange`, `label`, `idPrefix`, `showModifier?`, `showType?`. Numeric segments force `text-parchment-900`. Use anywhere a form edits a decomposed roll spec. |

The campaign item form (`features/entities/CampaignItemsPanel.tsx`) is the reference consumer: it recomposes into five labelled fieldsets (Identity, Category details, Magic, Value & weight, Description + DM notes) with progressive disclosure — the versatile die and range appear only when relevant, the coin breakdown lives behind a `Disclosure`, and attunement/unique + the rarity value hint show only for a non-Mundane rarity. Category details also carries the worn-slot authoring UI (#572): a `Slot` `Select` (from `WORN_SLOTS`/`wornSlotItemKindLabel`, defaulting to "Carried (not worn)" → `slot: null`) appears only for gear, while weapon/armor show a read-only "Equips to: …" line derived from `allowedSlotsForItem`; switching category away from gear clears the chosen slot in form state (mirrors the backend clear in #571).

## Iconography — `components/ui/icons.ts`

All icons resolve through `components/ui/icons.ts`. Two libraries: `lucide-react` for UI chrome (kebab, chevron, search, +/−/✕) and `react-icons/gi` (game-icons) for D&D flavor (abilities, item categories, empty-state heroes). Always use per-icon subpath imports (`lucide-react`, `react-icons/gi`) — never `react-icons/all`. Domain→icon lookups are typed `Record<…, IconType>` maps (`ABILITY_ICONS`, `ITEM_CATEGORY_ICONS`) so a missing/renamed key is a compile error; `icons.ts` is the single allowed `components/ui` file that imports `@/types/character` for exactly this reason. Icons are monochrome `currentColor`: never set `fill`/hex — give the icon a parent with a `text-*` token so it works in light and dark. Decorative icons get `aria-hidden="true"`; icon-only buttons keep their `aria-label`. **No colorful emoji in the UI** (#496): render a lucide/game-icon component instead — e.g. `Lock` for a Hidden/DM-notes badge, `Plus` on New/Create buttons, `Zap` for the Action Surge accent, `VenetianMask` for secret-identity merges. The barrel re-exports these lucide names (`Lock`/`Plus`/`Zap`/`VenetianMask`) alongside the game-icons. Monochrome text glyphs are fine and intentionally kept: the `✕` close control and the `⚑` future-feature marker.

## Loading — delay-gated, never flashing

Loading indicators are **delay-gated**: a fast fetch (the common case) must render
**nothing** — no text, no spinner — so it can't flash up and vanish. Only a load
still pending after a threshold shows feedback. The mechanism is one hook,
`hooks/useDelayedFlag.ts`:

```ts
const showSpinner = useDelayedFlag(isLoading);   // true only after isLoading
…                                                 // stays true for 400ms (default)
{isLoading && (showSpinner ? <Spinner /> : null)}
```

`useDelayedFlag(active, delayMs = 400)` returns `true` only once `active` has been
continuously `true` for `delayMs`, and resets to `false` immediately when `active`
clears. Feed it the existing "is loading" expression (e.g. `character === undefined
&& !error`, `catalog === null && !catalogError`); keep the surrounding
loading/error/empty/content branching unchanged — only the indicator JSX is gated.
Pair it with the `Spinner` primitive: `variant="page"` for full-screen route loads
(centered), `variant="inline"` (default) for loads inside an already-rendered
panel/modal. **Never** render bare `Loading…` text as a stand-in for a spinner.

## API calls — `client.ts` is the only call site

All `fetch` calls go through `frontend/src/api/client.ts`. **Never call `fetch` directly from a component.**

When adding a new backend endpoint:
1. Add the function to `client.ts` — delegate to the generic `request<T>(path, init, "Failed to …")` (JSON reply) or `send(path, init, "Failed to …")` (void/204), so you inherit the shared ok-check/error-parse/throw flow instead of re-hand-rolling it. Intent-bearing `…/transactions` endpoints go through `postTransactions`.
2. Import it in the component

This keeps all URL construction, error handling, and type casting in one place.

Every domain call goes through `apiFetch`, which adds `credentials: "include"`
(so the `cs_session` cookie flows cross-origin in dev: 5173 → 4000) and routes
any **401** to a single registered handler — `setUnauthorizedHandler` — instead
of per-call handling. `AuthProvider` registers that handler to drop auth state to
anonymous (the router then shows login). The auth bootstrap `fetchMe` uses a
plain credentialed `fetch` so its expected 401 ("not signed in") doesn't trip
the global handler. Auth functions: `fetchAuthProviders`, `fetchMe`, `logout`.

## Auth — `features/auth/`

OAuth-only (no passwords). Pieces:
- **`AuthProvider.tsx`** — context (`useAuth`): bootstraps from `fetchMe`
  (`loading → authenticated | anonymous`), exposes `user` + `logout()`, and
  registers the client's unauthorized handler.
- **`AuthGate.tsx`** — renders the app only when authenticated; a loading
  placeholder during the probe; `LoginPage` for anonymous (so a 401 anywhere
  lands on login, never a white screen).
- **`AppHeader.tsx`** — slim chrome that renders only the avatar-triggered `AccountMenu`.
- **`AccountMenu.tsx`** — composes `DropdownMenu` + `Avatar`; the avatar is the trigger. The open panel holds a presentational identity row (name + email, no `menuitem` role), an **Appearance** section (three `menuitemradio` rows Light/Dark/System with lucide `Sun`/`Moon`/`Monitor` icons; the active one is `aria-checked` + `Check`-marked; selecting calls `setPreference` and does not close the menu), and a danger Log out `menuitem` (calls `logout()` then `close()`).
- **`pages/LoginPage.tsx`** — buttons are **data-driven** from
  `GET /api/auth/providers` (each a plain anchor to its `startUrl`), so enabling
  a provider server-side needs no frontend change.

`App.tsx` order: `BrowserRouter → ErrorBoundary → ThemeProvider → AuthProvider →
AuthGate → (AppHeader + Routes)` — ErrorBoundary stays outermost over the app
content; ThemeProvider wraps auth so `data-theme` applies app-wide.

## Theme / dark mode — `features/theme/`

Dark mode is a set of `--color-*` overrides under `[data-theme="dark"]` in
`index.css` (the dark palette uses reversed ramps + dark shadows + a
`--color-backdrop` token; #211 — see `design_system.md`; light mode is
unchanged). The preference is persisted at `localStorage["cs:pref:theme"]`,
defaulting to `system`.

- **`hooks/useThemePreference.ts`** — pure persistence + resolution:
  `ThemePreference = light | dark | system`, `loadThemePreference` /
  `saveThemePreference`, `getSystemTheme` (via `matchMedia`), `resolveTheme`, and
  the `useThemePreference` hook (a `useState`-shaped pair).
- **`features/theme/ThemeProvider.tsx`** — context (`useTheme`): exposes
  `{ preference, resolved, setPreference }` and writes the resolved theme onto
  `document.documentElement.dataset.theme`; while on `system` it re-resolves on
  OS `prefers-color-scheme` changes.
- **`index.html`** — a tiny blocking inline script applies `data-theme`
  pre-paint to avoid a flash of the wrong theme (the storage key is duplicated
  there from `useThemePreference.ts`).
- **`AccountMenu.tsx`** — hosts the direct theme selector: an Appearance section
  with Light/Dark/System `menuitemradio` rows that pick a `preference` directly
  (no cycling), inside the account dropdown.
- Input/control surfaces use `bg-parchment-50` (never `bg-white`) so they flip in dark mode; paired `text-parchment-900`/`placeholder:text-parchment-400` tokens already flip (#212).

## Dice engine

`frontend/src/lib/dice.ts` is the **only** place `Math.random` is called for dice. Key exports:

```typescript
rollDie(faces: number): number          // the sole Math.random call
rollSpec(spec: RollSpec): RollResult    // rolls all dice + sums + applies modifier
summarizeRoll(values, spec): RollResult // for when values come from outside (physics roller)
formatRollSpec(spec): string            // "3d6 + 2", "4d6 drop lowest", "1d20 + 5 (advantage)"
usesAdvantage(spec): boolean            // the advantage/disadvantage guard (below)
```

`RollSpec`: `{ count, faces, modifier?, dropLowest?, mode?, crit? }`.

**Critical hit** (`crit?: boolean`, #731). RAW: a crit doubles the weapon damage **dice**, not the flat modifier. `rollSpec` doubles `count` when `crit` is set, leaving `modifier` single and `dropLowest` intact; `formatRollSpec` shows the doubled count plus a ` (crit)` suffix (e.g. `1d8 + 3` → `2d8 + 3 (crit)`) so the Session Log reads honestly. Crit never combines with the advantage-d20 path — the `usesAdvantage` guard is checked first, and a damage spec is multi-die anyway. `attackMath.critDamageSpec(spec)` builds the crit variant, reused for both the weapon spec and its dice-valued `DamageRider`s; the Battle Master superiority die is a flat add (`ManeuverPrompt`) and does **not** double. `AttackStepCard`'s Damage step rolls it through the same `logRoll` path — a nat-20 auto-doubles and a "Crit!" verdict flips the button to "Roll crit damage" (the standalone Critical button is gone since #811/#813).

**Advantage / disadvantage** (`mode?: "normal" | "advantage" | "disadvantage"`, #459). `rollSpec` honors `mode` **only for a single d20** — the `usesAdvantage` guard requires `faces === 20 && count === 1`, so multi-die damage specs (`2d6`) and non-d20 dice ignore `mode` and roll normally. An advantage roll rolls **2d20** and keeps the higher (disadvantage: the lower); the un-taken die stays in `RollResult.dice` flagged `dropped: true` (same mechanism as `dropLowest`) so toast + 3D can show both dice. Ties keep exactly one die.

The manual toggle is `features/dice/RollModeToggle.tsx` — a sticky Normal/ADV/DIS control (mounted alongside `RollResultToast` in `CharacterSheetPage`/`SessionPage`) that sets `mode` in `RollContext`; `roll()` merges that mode into every eligible spec (a caller-pinned `spec.mode` wins). Placement is pure-CSS per breakpoint (#770, same split-by-`md:` pattern as #746): at `< md` it renders a fixed full-width **docked bar** (`inset-x-0 bottom-0`, parchment surface, top border, `env(safe-area-inset-bottom)` padding) so it never floats over a control; at `md:` the surface strips back to the original `bottom-6 left-6` floating pill. Both mounting pages add a `pb-[calc(4rem+env(safe-area-inset-bottom))]` bottom gutter so the last content row clears the bar — `CharacterSheetPage` drops it at desktop (`md:pb-0`), while `SessionContent` uses `md:pb-6` to preserve its previous `py-6` bottom rhythm. The `role=group` / `aria-label="Roll mode"` / `aria-pressed` control renders once (no per-breakpoint double-render); z-index is unchanged, so an open `BottomSheet` sits over the bar. `RollResultToast` shows both dice (dropped one struck through), an Advantage/Disadvantage label, and keeps natural-20/1 highlighting on the **taken** die; `DiceRoller` renders both d20s.

**Toast visibility (#801).** `RollResultToast` is `createPortal`'d to `document.body` so its stacking is deterministic, and it returns `null` while **any** dialog is open — `hooks/useDialogChrome.ts` keeps a module-scoped open-dialog count (every `Modal`/`BottomSheet` increments on mount / decrements on unmount) exposed via `useAnyDialogOpen()`. Suppression is **everywhere, not gated to `< md`**: mobile sheets and the desktop centered modal both draw a full-screen backdrop-blur scrim that would occlude the corner toast, and a roll fired from inside a dialog already shows its result on the sheet (the attack card). No z-index change — suppression, not escalation. Rolls fired while a dialog is open still log to the session log via the existing `useRollLogger`/`logSessionRoll` path.

**Roll affordances → `RollContext`.** `RollProvider` (mounted once per page in `CharacterSheetPage`/`SessionPage`) exposes two roll paths that share the sticky `mode`:
- `roll(spec, label)` — instant fast-roll (no 3D). Used by the in-combat attack/damage/spell pickers, which run their own `logRoll` calls.
- `rollAnimated(spec, label, log?, onSettled?)` — plays the 3D `DiceRollModal` overlay, publishes to `RollResultToast`, and (when `log` is set) emits the roll's category event. This is what `RollButton` uses, so the sheet's **skill checks** (`SkillsTable`), **ability checks + saving throws** (`AbilityScoreBox`), and **initiative** (`BannerVitals`) all animate and log via the `log={{ kind, source, ability?, skill?, dc? }}` prop (#460). `onSettled(result)` fires with the settled `RollResult` so a caller can apply the exact **shown** roll server-side — `UseConsumableButton` (#121) uses it to forward a consumable's rolled effect-die values via the `use` op's `rolls`, so the animated roll equals the applied heal.

`logSessionRoll` is the shared **best-effort** logging path (a no-op unless `RollProvider` was given both `characterId` and an active `sessionId` — rolls only log inside a session, like attack/damage). `ConcentrationSaveModal` calls it directly so a manual concentration CON save reaches the Session Log as a `saveRoll` too. `RollProvider`'s `onRollLogged` bumps the session-log refresh key.

**State-driven advantage/disadvantage (#486).** `RollProvider` also carries `rollModifiers` (the character's derived `RollModifier[]` from active conditions + buffs — backend `serializeCharacter`; each is `{mode, kind: attack|check|save|initiative, ability?, source}`), passed in by `CharacterSheetPage`/`SessionContent`. The pure resolver `lib/rollMode.ts` `resolveRollMode(rollModifiers, {kind, ability?}, manualMode)` → `{mode, sources}` merges the grants that apply to a given roll: advantage + disadvantage from **different** sources cancel to `normal` (RAW), and the sticky manual toggle (`mode` from `RollContext`) is an explicit player override that short-circuits every auto grant. `rollModeChip(resolved)` builds the "why" string (`"disadvantage — Poisoned"`, or `"normal — Rage, Poisoned"` when they cancel). `RollButton` calls the resolver off its `log={{kind, ability?}}`, pins the resolved `mode` into the spec (a caller-pinned `spec.mode` still wins), and renders the chip below the affordance — so checks/saves (`AbilityScoreBox`), skills (`SkillsTable`), and initiative (`BannerVitals`) auto-resolve. The session attack flow resolves once per render in `useAttackRolls` (kind `attack`, not ability-scoped), pins the mode into `handleAttack`'s roll, and exposes `attackChip` on the `AttackEntryView` for `AttackStepCard`'s Roll-to-hit step. First consumers: Rage (advantage on Strength checks & saves) and Poisoned (disadvantage on attacks & ability checks); self-scope only.

**3D rollers** (`features/dice/DiceRoller.tsx` scripted, `features/dice/PhysicsDiceRoller.tsx` physics) both produce a `RollResult` shape via `summarizeRoll` — they're interchangeable via the shared `DiceRollerProps` contract in `features/dice/diceRollerTypes.ts`. In tests, stub `@/features/dice/DiceRoller` (it mounts a Three.js Canvas that won't render in jsdom) to fire `onResult` on mount — see `RollContext.test.tsx` / `ConcentrationSaveModal.test.tsx`.

**Lazy 3D stack (#432).** The three.js/@react-three/cannon-es/troika stack is heavy, so it never sits in the initial bundle. The two dice seams on eager pages load it via `React.lazy` behind `<Suspense fallback={null}>` — `RollContext` lazy-loads `DiceRollModal`, and `ConcentrationSaveModal` lazy-loads `DiceRoller` — so the vendor chunk is fetched only when a roll animates. Character creation's `PhysicsDiceRoller` rides the route-lazy `CharacterCreatePage` chunk instead. Because the tests stub `@/features/dice/DiceRoller` and the lazy import resolves a tick later, assert the roller with `findByTestId` (async), not `getByTestId`. See the Bundle splitting note below for the matching `manualChunks` config.

The dice face numbers are drei `<Text>` (troika). Two things keep them working under the single-origin CSP (#408), which local split-origin dev never exercises:
- **Main-thread typesetting** — `lib/troikaTextConfig.ts` (`configureDiceText()`, run at module scope in `features/dice/DiceScene.tsx` so it fires when the lazy dice chunk evaluates, before any `<Text>` renders — kept out of `main.tsx` so it doesn't pin troika into the initial bundle, #432) sets troika `useWorker: false`. Troika's default worker rehydrates via a `blob:` `importScripts`, which the CSP `script-src` blocks (`worker-src` doesn't cover `importScripts`), so `<Text>` would otherwise suspend forever and stall the whole roller.
- **Bundled font** — `DieMesh` passes an explicit `font` (a same-origin `@fontsource/source-sans-3` **woff**, imported so Vite hashes it into `dist`). Without it, main-thread troika fetches unicode-font-resolver data from a CDN, which the CSP `connect-src` blocks. woff, not woff2 — troika's parser can't read woff2.

Defense-in-depth: `DieMesh` keeps its `<Text>` in its own `<Suspense>` and `DiceScene` renders the physics rig **outside** the cosmetic-environment `<Suspense>`, so a text/env load can never suspend away the sim that produces the result (this decoupling is what makes the roll survive even if the labels fail).

## Bundle splitting

The initial JS bundle is kept small (#432) by two levers that work together — one alone isn't enough, since a chunk is preloaded whenever the entry can reach it through a *static* import:

- **Lazy seams (dynamic `import()`)** break static reachability. `App.tsx` route-lazies the heavy non-initial pages (`SessionPage`, `CharacterCreatePage`) via `React.lazy`, and the dice seams above lazy the 3D rollers. Nothing eager imports the three.js stack.
- **`manualChunks` (in `frontend/vite.config.ts`)** isolates the vendors into two chunks: `dice-vendor` (three / @react-three / cannon-es / troika) and `react-vendor` (react, react-dom, scheduler, react-router). Both are needed: without a dedicated `react-vendor`, Rollup folds React into `dice-vendor`, and the entry's static React import then drags the whole 3D stack back into the initial preload. The Vite `preload-helper` (imported by every `React.lazy` call site) is likewise pinned to `react-vendor` for the same reason.

Net effect: `dice-vendor` (~1.1 MB) is fetched only when a roll animates or character creation loads — verify by confirming it is **not** a `modulepreload` in the built `dist/index.html`. `build.chunkSizeWarningLimit` is raised past `dice-vendor` on purpose (it never gates first paint); the warning still guards the initial `index`/`react-vendor` chunks.

## Feature-orchestrator split convention

Large interactive sections follow the orchestrator/row pattern:

```
<OrchestratorComponent character={character} onUpdate={setCharacter}>
  // owns state, batches API calls via apply*Transactions, re-renders via onUpdate
  // renders the Card and section header

  <RowComponent op-handlers-as-callbacks />
  // presentational only — no direct API calls, no local async state
  // fires callbacks up to the orchestrator
```

Examples:
- `features/inventory/`: `InventoryList` (orchestrator; also owns the derived **X/3 attunement** readout, shown when any held item is attunable). It composes three cluster hooks — `useItemCatalog` (catalog fetch), `useInventoryTransactions` (pending/error + the `applyOps`/`submitOperations` write path), `useSellSelection` (multi-select sell flow) — and delegates presentation to `InventoryHeaderActions` (title accessory; hosts `SellModeActions`, the "N selected · ~X gp · Sell · Cancel" bar shown while multi-select sell is active), `InventoryMeters` (encumbrance + attunement), `InventoryToolbar` (search + filter chips), and `InventoryBody` (routes to the sell panel / empty state / pack view) — whose pack view is `InventoryContent` (Worn paper-doll vs the Bag's `InventorySections` category-sectioned rows) — plus `CurrencyEditor` (denomination inputs live in `CurrencyEditForm`), with the filter/section math in `lib/inventorySections.ts`. / `InventoryRow` (delegates to `InventoryEditForm`/`EquipToggle`/`AttuneToggle`/`UseConsumableButton`/`ActivateControl`/`ItemSummary`/`ItemProse`) / `AddItemPanel` / `SellPanel`. `AttuneToggle` (#546) is the attune/unattune pill, shown only for `requiresAttunement` items; it's disabled when 3 items are already attuned (`atCap`, passed down from `InventoryList`) and the server's prereq/cap rejection surfaces through the list's error line. `UseConsumableButton` (#121) is the Use affordance shown only for `category === "consumable"` rows; it plays the 3D effect-dice roll (forwarding the shown values to the server) and, for charged consumables, renders an X/Y charge indicator, disabled at 0 until a long rest recharges. `ActivateControl` (#543) is the activate/deactivate toggle for an item's `activatedEffect` capability, shown while the item is equipped/attuned; it surfaces remaining uses + the activation/duration reminder and disables at 0 uses until the matching rest recharges. `ItemSummary` renders the attunement state ("Attuned" / "Requires attunement"), a gold chip per `passiveBonus` capability (label via `capabilitySummary`), and — for an item with a charges pool (#555) — a `remaining/max charges` pill (arcane tone; parchment-outline at 0) whose `title` is the server-derived recharge text (`item.charges.recharge`). `LoadoutList` (#925, replaced the rejected `EquipmentDoll` tile grid) is the interactive **Worn** view, reached via `InventoryList`'s Bag ⇄ Worn `Segmented` toggle: a grouped list of rows — **Weapons / Armor / Accessories** (the renamed Hands/Armor/Adornment slot groups) — with an **Attuned N/3** header. A filled row shows the slot glyph · item name · slot label · rarity `Badge` · a gold proficiency warning glyph (sr-only "Not proficient") · a versatile-grip `Badge` (main-hand only), plus a trailing `Popover` with Unequip / Swap (Swap → a `SlotPickerPanel action="replace"` of slot-compatible bag items). An empty row collapses to a "＋ Equip" `Popover` → `SlotPickerPanel action="equip"`; a two-handed main-hand renders a static "Held by … (two-handed)" locked off-hand row. It consumes the `equip { inventoryItemId, slot }` op from #565 and batches an unequip+equip for swaps (toasting the returned item). Rows requiring attunement render the reused `AttuneToggle` (disabled at the derived 3-item cap). The grouped-rows model + derived proficiency/grip/attunement data live in `lib/loadout.ts` (`buildLoadoutGroups`, `attunementSummary`); slot placement + `isProficientWithItem`/`versatileGrip` stay in `lib/paperDoll.ts`; slot glyphs come from `EQUIP_SLOT_ICONS` (`components/ui/icons.ts`, game-icons.net via react-icons/gi, CC BY 3.0 — credited on `/about`).
- `features/spells/`: `SpellsSection` (orchestrator; holds a ref to the `SpellbookList` grimoire so the block's Manage-spellbook opener can scroll to it) / `SpellcastingOverview` — the **arcane-keyed spellcasting block on the record** (#893, `caster-spellbook.html §1`): a ruled-ledger `Card` composing `SpellcastingStatBar` (boxed **Save DC · Spell Attack · Prepared X/Y** stat boxes; the Prepared box hides when there is no prepare mechanic) / `SpellStatusBanners` / `SpellSlotMeters` (per-level **pips** — a filled pip expends, a spent pip restores; keeps the Pact Magic / Mystic Arcanum groupings) / `PreparedSpellList` (at-will cantrips + prepared leveled spells, each with a **Cast** affordance reusing `handleCast`) / the inline cast-result + error banners / a dashed **Manage spellbook →** opener. Pure helpers: `lib/preparedSummary.ts` (`derivePreparedSummary`) and `lib/preparedSpells.ts` (`derivePreparedCastable`). / `SpellRow` (composes `SpellRowBadges`→`SpellItemBadges` / `SpellRowActions` / `SpellSlotPicker` / `SpellRowDetails`; derivations in `lib/spellRow.ts`) / `AddSpellPanel` (tab shell composing `SpellCatalogTab`→`SpellCatalogRow` and `CustomSpellForm`→`CustomSpellEffectFields`→`CustomSpellDamageFields`; catalog fetch in the co-located `useSpellCatalog` hook, pure logic in `lib/addSpell.ts`)
- `features/class/`: `ClassFeaturesSection` (orchestrator) composes per-feature blocks — `ClassRosterSection` / `SubclassSection` / `ResourcePoolsSection` / `ManeuversSection` / `FightingStyleSection` / `ClassFeaturesList` plus the subclass sections (`DisciplinesSection`/`ShadowArtsSection`/`ChannelDivinitySection`/`CloakOfShadowsSection`). Its shared busy/error transaction runner is the co-located `useClassTransactions` hook; all gating/derivation (roster, `needsSubclass`, entitlement flags, empty-state) is pure in `lib/classFeatures.ts` (`deriveClassFeatureView`).

The orchestrator pattern keeps async state and API batching in one place and makes rows easy to unit-test in isolation — pass mock callbacks, assert they fire with the right args. See `testing.md` for component test patterns.

## Quick-capture surfaces — `features/journal/CapturePalette.tsx`

The fast in-session note surface. The sheet no longer edits the journal at all (#867): its only journal surface is the read-only `features/journal/JournalDoorway.tsx` closed-book card that links to the journal page — jotting on the sheet is done through this ⌘J quick-capture instead. `hooks/useCaptureHotkey.ts` registers a document-level **⌘J/Ctrl+J** listener (held in a ref so it binds once) that **toggles** the capture surface; both `CharacterSheetPage` and `SessionPage` (via `useSessionLifecycle.toggleCapture`) use it. The hotkey no-ops when a modal dialog is open or when focus sits in an editable element **outside** the dock — the dock marks its subtree with `data-capture-dock` so both guards (and its own Esc handler) can exempt it. Each page also renders a visible **"＋ Note"** header button that opens the same surface (#274) — the discoverable, touch-friendly affordance; the shortcut stays a power-user accelerator. **Presentation is per-breakpoint (#771)** via `hooks/useIsBelowMd.ts` (`matchMedia("(min-width: 768px)")`, lazy-initialized so the first paint picks the right surface):

- **Below `md` (#866)** — a **full-height, keyboard-pinned chat surface** (`features/journal/MobileCaptureSheet.tsx`): a `role="dialog"` `aria-modal` panel portalled to `document.body`, pinned to the visible viewport via `hooks/useVisualViewport.ts` (`{ height, offsetTop }` off `window.visualViewport`, rAF-coalesced so the keyboard-animation event burst re-lays-out once per frame; applied as inline `height` + `translateY`) so the composer **docks flush above the iOS keyboard** with ≥3 recent notes still visible. It shares `useDialogChrome` (focus-trap/Escape/open-dialog count) and keeps the #784 focus discipline (deferred double-`requestAnimationFrame` + `{ preventScroll: true }` + `scrollTo(0,0)`). **iOS scroll-lock (#877):** `useDialogChrome`'s `overflow: hidden` body-lock is a no-op on iOS (WebKit scrolls the whole layout viewport up to lift a focused input above the keyboard, desyncing the pinned panel from the page behind and bleeding sheet content through the gap), so the surface layers a **real** body-lock via `hooks/useMobileScrollLock.ts` — `position: fixed; top: -{scrollY}` on `document.body`, restored + scroll re-applied on unmount — called *before* `useDialogChrome` so its scroll-restore cleanup runs *after* the dialog's focus-restore. The full-bleed `fixed inset-0 bg-parchment-50` backdrop then sits reliably *behind* the panel as a safety net (parchment shows in any region the panel doesn't cover, never the sheet). The lock is capture-specific so `Modal`/`BottomSheet` are unaffected. Header = small-caps "Quick capture" · optional serif session title · a garnet **"Done"** close button; the `MobileFeed` runs newest at the **bottom** (`justify-end`, auto-anchored to the bottom on open and after each save via a note-count-keyed effect) under a **"Tonight ·"** day divider; the composer uses `GrowingComposer variant="mobile"` (inline lock icon-button + ≥44px circular send). Dark = the "Campfire" register automatically via token flips, plus a **dark-only gold radial glow** low in the feed (`[data-theme="dark"] [data-mobile-capture-feed]` in `index.css`). The old `BottomSheet` mobile path is retired here; `BottomSheet` itself is untouched for its other consumers.
- **`md`+ (#865)** — a **non-modal margin dock** (`features/journal/CaptureDock.tsx`): a fixed 370px right panel (parchment surface, dashed-gold stitch seam on its left edge, `role="dialog"` **without** `aria-modal`), portalled to `document.body`. **Non-modal by design** — no scrim, no body scroll-lock, so the sheet behind stays scrollable/clickable while you jot beside it. Its header shows a small-caps "Quick capture", the live session's title + a minute-ticking elapsed time (`useElapsed`, only when a `Session` prop with `status: "active"` is passed), and a "Close · ⌘J" button. `useDockChrome` defers initial focus to the composer, closes on Escape **only while focus is inside the dock** (the mention popover swallows its own Escape first), and restores focus to the opener on close. The `DockFeed` runs newest-at-**bottom** grouped under day dividers, with the growing composer docked below it.

Both surfaces share **`features/journal/GrowingComposer.tsx`** — a reusable, standalone pill→ruled-card composer (also consumed by the #866 mobile rewrite). It starts one line tall as a rounded-full pill and, as text wraps past one line (measured from the editor's `scrollHeight`/line-height via a `ResizeObserver` + `useLayoutEffect`), relaxes into a serif **ruled writing card** — a faint rule per 27px line via a `background-attachment: local` repeating gradient — growing to `maxLines` (8) then scrolling internally. It floors its font at `text-base md:text-[15px]` so focusing the field can't trip iOS Safari's <16px auto-zoom. Enter saves / Shift+Enter newlines / an IME-commit Enter (`isComposing`) is ignored; the `@`-mention `MentionAutocomplete` is anchored **above** (`popoverPlacement="above"`) since the composer sits at the panel's bottom edge. A `variant` prop switches the control arrangement: `"dock"` (default) stacks the field over a control row with a `PrivateToggle` checkbox + keyboard hint + a compact circular send; `"mobile"` (#866) lays a compact **lock icon-button** (`PrivateLockButton`, `aria-pressed`) · field · a **≥44px** circular send in one keyboard-docked row and drops the hint. It owns its draft + Private state, and on a successful save clears the field, resets Private, and refocuses the editor. It commits via `onSave(body, visibility?)`, wired by `CapturePalette` to `createJournalEntry({ kind: "NOTE", body, sessionId, visibility? })`; the returned Character propagates through the page's `onUpdate`.

**Visibility opt-out (#838):** for a campaign character the composer shows a `PrivateToggle` checkbox (default off = shared); a default save omits `visibility` (the server defaults in-campaign notes to `CAMPAIGN`), a toggled save sends `visibility: "PRIVATE"`, and a successful save resets the toggle so privacy never leaks into the next note. Campaign-less characters get no toggle (their writes are always PRIVATE server-side). `PRIVATE` rows show a `Lock` icon (`aria-label="Private note"`) in the feed and `JournalEntryPanel` surfaces. The feed rows live in **`features/journal/NoteFeed.tsx`** (`MobileFeed` #866 + `DockFeed`, both newest-at-bottom under day dividers — `MobileFeed` uses a "Tonight ·" divider), which share `NoteRow` (display row + lock + `formatJournalTime`), `NoteEditor` (inline edit; in a campaign it carries its own `PrivateToggle`, initialized from the note, and always patches an explicit `visibility`), `PrivateToggle` + the `PrivateLockButton` icon toggle, and one `useFeedRowState` machine (which row is editing/delete-confirming, cleared on a truthy result). `features/journal/JournalEntryPanel.tsx` mirrors the same visibility rules for the journal page's add/edit flows. The busy/error state and the create/update/delete client calls live in the shared **`features/journal/useJournalMutations.ts`** hook, used by the journal-page surfaces (ManuscriptPage, CapturePalette) — extend journal CRUD there, not in the components; `update(entryId, patch)` takes a `{ body?, visibility? }` patch object, not a bare body string.

## Entity registry & @-tagging — `features/journal/` + `features/entities/` (#248)

The shared campaign wiki surface. Three pieces, all scoped to `character.campaignId`:

- **`features/journal/MentionAutocomplete.tsx`** — a **contenteditable** wrapper that drives the `@…` autocomplete and renders each stored `@[<uuid>]` token as an atomic `@Name` chip while editing (#248/#269). Public contract is unchanged: `value` (raw `@[<uuid>]` body) in, `onChange(rawBody)` out — the DOM is serialized back to tokens on every input via `lib/mentions.serializeMentionDom`, so hosts and entity backlinks are unaffected. Selecting a match or running "➕ Create <Type> …" inserts a chip; Backspace/Delete next to a chip removes it atomically and pins the caret to the chip-start body offset (via `serializeMentionDomBeforeCaret` + `placeCaretAtBodyOffset`, deferred past the value re-render) so deletion lands deterministically cross-browser (#273). Follows the WAI-ARIA combobox pattern: the `role="textbox"` editor carries `aria-controls` → the `role="listbox"` popover and `aria-activedescendant` → the active `role="option"`'s stable id (each option `id={listboxId}-opt-<index>`, driven by `activeIndex`); keyup skips the popover nav keys so arrowing doesn't reset the highlight. It intercepts Up/Down/Enter/Esc *only while the popover is open* (Enter selects a match instead of submitting; Esc `stopPropagation`s so it closes the popover, not the palette), and falls through to the composer's own `onKeyDown` otherwise. No `campaignId` → a "create or join a campaign" CTA instead of matches. Wired into `CapturePalette` (NOTE) and `JournalEntryPanel` (NOTE body). An `EXECUTED`-merged identity stays offered but is annotated with its ultimate survivor's name ("Jenkins → Vecna") via `useCampaignMerges` + `lib/merges.ultimateSurvivorName` (#387). Decomposed (#609) into a thin composition shell plus two co-located pieces: **`useMentionEditor.ts`** (the contentEditable/chip DOM wiring — editor ref, `value`⇄DOM sync effect, trigger + suggestion model via inner `useMentionSuggest`/`useEntityCreation`/`useMentionHandlers` hooks, and every insert/delete/keyboard/paste handler; pure string/DOM math delegates to `lib/mentions.spliceMentionToken` / `resolveAdjacentChip`) and **`MentionSuggestionList.tsx`** (the `role="listbox"` popover — matches + the "➕ Create …" row; each match row is an inked small-caps name + a type-colored diamond, #862, matching `MentionText`'s ink, with the type kept in an `sr-only` label). The hooks are journal-only, so they stay co-located rather than moving to `hooks/`. **Per-breakpoint suggestion presentation (#785):** at `md`+ the list is the absolute `top-full` popover; below `md` it renders **in-flow** (relative, no `absolute`/`top-full`) as an `overflow-y-auto` scroll list capped to a share of the keyboard-aware `useVisualViewportHeight`, so on the mobile Quick-capture BottomSheet the suggestions sit between composer and sheet bottom instead of being clipped under the on-screen keyboard. `MentionAutocomplete` picks the variant via `useIsBelowMd` and passes `inFlow`/`maxHeight`; an optional `popoverPlacement="above"` flips the absolute list to `bottom-full` (used by both `GrowingComposer` variants, whose field sits at the panel's bottom edge — the dock #865 and the mobile capture #866). **`popoverPlacement="above"` also forces the absolute popover on mobile** (`inFlow = isMobile && placement !== "above"`): the mobile capture composer already docks flush above the keyboard, so an above-anchored popover opens up into the visible feed — never clipped — where the in-flow list would fall under the keyboard. The ARIA combobox wiring (`aria-controls`/`aria-activedescendant`/option ids) and keyboard nav are presentation-independent, and arrowing `scrollIntoView({ block: "nearest" })`s the active option in either variant. `JournalEntryPanel` inherits the same split for free (it reuses `MentionAutocomplete`).
- **`features/journal/MentionText.tsx`** — renders a stored body with `parseMentionBody`: text verbatim, each **resolvable** `@[<uuid>]` as an **inked name** (#862) linking to the entity detail page (name resolved AT RENDER, so a rename reflects instantly). Not a pill: mentions render as a scribe's inked name — small-caps, semibold, entity-colored text with a dotted underline at ~45% of the ink — via `MENTION_INK_BASE_CLASS` + the per-type `ENTITY_TYPE_INK_TEXT_CLASS`/`ENTITY_TYPE_INK_BORDER_CLASS` maps (in `lib/mentions.ts`). The font family **inherits** from context (serif in journal prose, sans elsewhere), so one component reads correctly everywhere it renders. An **unresolvable** id — a now-hidden entity a non-owner can't see (#379), or a deleted one — renders as a neutral small-caps 🔒 Hidden name (no pill), never the raw token. Replaces the raw `{body}` renders in `CapturePalette` and the `SessionSummaryModal` recap journal list. **Interactive app-wide (#843):** when `campaignId` is set, each resolvable mention's Link also carries `useEntityPreview` trigger props — desktop hover shows the shared `EntityPreviewCard`; click / mobile tap keeps the plain navigation. Preview data is hover-lazy via the `entityPreviewData` module cache, so rendering a long session log costs zero fetches; the redacted fallback and no-`campaignId` mentions stay fully inert (no link, no preview).
- **`hooks/useCampaignEntities.ts`** — fetches + module-level-caches the campaign entity list once, exposing an id→entity map for chip resolution. The list is server-filtered by role, so for a non-owner the cache holds only `REVEALED` entities — which is what makes `MentionAutocomplete` offer revealed-only matches and `MentionText` redact hidden refs, without any client-side visibility logic.
- **`features/entities/EntityDetailPage.tsx`** (route `/campaigns/:id/entities/:entityId`) — the wiki-article reading pane (#842). On `lg`+ a `lg:grid-cols-[300px_minmax(0,1fr)]` split view pairs `EntityPaneRail` (CSS-gated `hidden lg:flex` — not `useIsBelowMd` — sibling rows with a 32px `EntityPortrait` tile + `stats.mentionCount`, local search/type-chip filter state that survives row nav because only the pane swaps, `aria-current="page"` on the URL's row) with the article; below `lg` the article is full-page with a `← Codex` back link (`useEntityBackTo`: Manage origin via `location.state.from`/`?from=manage`, else Codex #489; in-app relative paths only). Loading/not-found render **inside the pane** so the rail never unmounts. The article composes `EntityArticleHeader` (serif H1 + type badge + owner-only 🔒 Hidden badge #379, italic alias line, "Edit entry"), the `notes` lead, `EntityInfobox` (right column at `xl`, stacked above the lead below — 96px `EntityPortrait` tile with an owner-only "＋ Add a portrait" affordance under an empty tile that jumps to the edit form via `onEdit` (#844), all-derived fact rows incl. an owner-gated "Character sheet →" link shown only when the viewer owns the linked PC's character, and the owner's quiet Reveal/Hide + Delete links #523), `EntityChronicle` (`chronicleGroups`/`splitChronicle` session-header timeline of `fetchEntityBacklinks`, latest 3 groups + "Show earlier sessions" expander; entry bodies render via `MentionText`, so chronicle chips inherit the #843 hover preview), `EntityConnections` (tinted co-mention chips from `fetchEntityConnections`, hidden when empty), and `EntityContributeBand` ("Add to this entry" → the inline `EntityEditForm` — type/name/aliases/Portrait URL/notes, blank Portrait URL saves as `null` #844 — also deep-linkable via `?edit=1`, the target of #841's "Add what you know →" CTA). State lives in `useEntityDetail` (fetches entities `{includeStats:true}` + campaign `characters` + connections). **Identity merges (#387):** an `EXECUTED`-merged entity shows a "Revealed to be @Survivor" banner with the full chain path; a survivor lists its "Former identities" and chronicle entries tagged as a former identity carry an "as {name}" label (via `useCampaignMerges` + `lib/merges`).
- **`features/entities/CampaignCodex.tsx`** (the hub's Codex tab, #367/#523/#840) — the campaign's **single** entity browse surface for every role, rebuilt as the full-width "Chronicle Ledger" shell (#840, replacing the old boxed `EntityList`): an orchestrator owning query/type-filter/sort/creating state over the `useCampaignEntities` cache, piping `matchEntitiesDetailed` → type filter → stats merge (by id from `useCodexActivity`'s stats list, #853) → `groupByInitial` (A→Z) or a comparator-ranked single pseudo-group (mention sorts) into a `lg:grid-cols-[280px_minmax(0,1fr)]` grid of `CodexRail` + `CodexLedger`, widened at `xl` to a third `280px` column holding `CodexActivityRail` (fed by `useCodexActivity`; below `xl` the rail collapses to the `NeedsChroniclingBanner` above the ledger, #841). Below `md` (`useIsBelowMd`) the rail's create toggle is replaced by a fixed FAB opening `EntityCreateForm` in a `BottomSheet`; a 0-entity campaign gets an `EmptyState` with a create-first-entry CTA, distinct from the empty-search "no match" message. Edit/delete/reveal-hide stay on `EntityDetailPage`.
- **`features/entities/CodexRail.tsx`** (#840) — the fully controlled browse rail: campaign eyebrow + "Codex" title + entry count (lg+ only), search input, type filter list with `ENTITY_TYPE_DOT_CLASS` tone dots + per-type counts (a vertical list at `lg`+, a horizontally scrollable chip row below with the search bar sticky above it), sort `Select` over `CODEX_SORT_OPTIONS`, and the "＋ New entry" toggle hosting `EntityCreateForm` as its inline panel (`children`).
- **`features/entities/CodexLedger.tsx`** (#840) — the annotated A→Z ledger: serif garnet letter dividers with hairline rules, rows linking to `EntityDetailPage` with a 44px `EntityPortrait` tile (#844), serif name + italic alias, one-line notes snippet (italic "No description yet" fallback), type Badge over a mention-meta line ("N ✎ · Session M" when stats exist, session part dropped without a `lastMentioned` ordinal, whole line dropped without stats, #853), a subtle "Matched in description" flag for notes-only search hits, and owner-only 🔒 Hidden rows at reduced opacity. An alphabet jump rail (`hidden sm:flex`) scrolls to letter sections via `window.scrollTo` + `getBoundingClientRect` (never `scrollIntoView`). Mention sorts (#853) receive one ranked pseudo-group and render it flat — no dividers, no jump rail. **Hover preview (#843):** one ledger-level `useEntityPreview` instance spreads trigger props onto every row Link and renders a single `EntityPreviewCard`; row navigation and keyboard behavior are untouched.
- **`features/entities/CodexActivityRail.tsx`** (#841) — the xl-only activity column, fully controlled (`{campaignId, statsEntities, activity}`; `CampaignCodex` owns the data via `useCodexActivity`, a hook that fetches `fetchEntities(…, {includeStats:true})` + `fetchEntityActivity` in parallel and deliberately does **not** prime the `useCampaignEntities` cache — plain-list primes would clobber the stats). Three cards: **Recently chronicled** (tone-dot timeline of mention/created items, relative `<time>` stamps with the absolute date as `title`, inviting line when empty), **Needs chronicling** (gold surface built directly — not `Card`, whose own `bg-parchment-50` vs a `className` override is stylesheet-order dependent — listing `needsChronicling` chips capped at 6 with an "Add what you know →" CTA to the first entity's `?edit=1`; hidden when empty), and **Most mentioned** (`mostMentioned` top-3 with ×N counts; hidden when empty). Entity chips are `Badge`s toned via `ENTITY_TYPE_TONE` wrapped in detail-page `Link`s. Also exports `NeedsChroniclingBanner`, the `xl:hidden` one-line complement rendered above the ledger.
- **`features/entities/useEntityPreview.ts`** (#843) — the hover-intent controller behind every entity preview: desktop-only (`(pointer: fine)` checked lazily on first enter — touch users keep plain tap navigation), 300ms intent delay before opening, and both data loads (`getPreviewStats` + `getPreviewConnections`) kick off only when the card actually opens. While open it dismisses on pointer-leave, any scroll (window capture listener), and Escape — the Escape handler is a capture listener that `stopPropagation`s so a `Modal`/`BottomSheet` underneath closes on the *next* Escape, not the same one. Returns `{ open, triggerProps(entity) }`; hosts spread `triggerProps` on the chip/row Link and render one `EntityPreviewCard`.
- **`features/entities/entityPreviewData.ts`** (#843) — JSX-free module cache for preview data: `getPreviewStats(campaignId)` (one `fetchEntities(..., { includeStats: true })` per campaign → id→`EntityStats` map) and `getPreviewConnections(campaignId, entityId)` (`fetchEntityConnections(..., { limit: 3 })` per entity), both inflight-deduped and cached for the page lifetime; a failed fetch caches empty so hover can't become a retry storm. `__resetEntityPreviewCacheForTests` mirrors `useCampaignEntities`'s test reset.
- **`features/entities/EntityPreviewCard.tsx`** (#843) — the presentational card: `createPortal` to `document.body`, `position: fixed` beside the anchor rect (prefers right of the anchor, flips left near the viewport's right edge, clamps vertically), `z-50`. It is a **pure enhancement** — `aria-hidden`, `pointer-events-none`, never focusable; keyboard users lose nothing (row/chip Enter still navigates — the footer's "Open ↵" is a hint, not a control). Content: 44px `EntityPortrait` tile (#844), serif name + italic aliases, `line-clamp-3` notes (italic fallback), top co-mention `Badge`s, 🔒 Hidden badge on a HIDDEN entity (owner view), and a "N mentions · last in Session M" footer.
- **`features/entities/EntityPortrait.tsx`** (#844) — the shared entity portrait tile behind every codex surface (infobox 96px, ledger + hover preview 44px, pane rail 32px — size/text classes come in via `className`): renders the `portraitUrl` image clipped to the rounded tile when set, else the type-tinted serif monogram (`monogram` + `ENTITY_TYPE_MONOGRAM_CLASS`). **Fallback rule:** a failed image load (`onError`) falls back to the monogram — never a broken-image glyph — keyed on the failing URL so a subsequent URL change re-attempts the image without a remount. `aria-hidden`; the adjacent text carries the name for AT.
- **`features/entities/EntityCreateForm.tsx`** (#840) — the create panel extracted from `CampaignCodex` so the desktop rail and mobile bottom sheet share it: type/name/aliases/Portrait URL/notes fields (`portraitUrl` sent only when non-empty, #844), document-level Escape-dismiss, owner-only "Start hidden from players" checkbox (sends `visibility: HIDDEN`; the backend gates visibility to owners); on success calls `createEntity` then `primeCampaignEntities` so the list, tab badge, and live journal chips update without a reload, then `onClose` — the host returns focus to its toggle.
- **`features/entities/CampaignManagePanel.tsx`** (the hub's owner-only Manage tab, #379) — since #523 the DM's **identity-merge** workflow only (the entity list + create/reveal/hide/delete moved to the Codex + `EntityDetailPage`): a "Prepare merge" form (pick old identity + true identity + note → `prepareEntityMerge`), and a merges list with Execute-reveal (confirm calls out the survivor auto-reveal, primes the entity cache) + Unmerge; writes `primeCampaignMerges`.
- **`features/entities/CampaignItemsPanel.tsx`** (the Manage tab's **second card**, below `CampaignManagePanel`, #380) — DM campaign-item authoring. A "➕ New item" form with two paths: **clone-from-catalog** (a `fetchItems` dropdown that pre-fills name/category/weight/cost/description + the matching detail block via `formFromCatalog`) and **from-scratch** with **category-conditional** detail fields — **weapon**: dice count/faces/modifier/type, versatile dice, 7 property flags (finesse/light/heavy/two-handed/reach/thrown/ammunition), rangeNormal/rangeLong, weaponClass, weaponRange; **armor**: category, base AC, dexModifierApplies, Max Dex bonus, stealth disadvantage, strength requirement; **consumable**: effect dice/modifier/description — plus a full 4-denomination cost (cp/sp/gp/pp, not gp-only) (#527). Each create (`createCampaignItem`) auto-registers a hidden ITEM entity; per-item reveal/hide toggles the fronting entity (`updateEntity({ visibility })`) and delete (`deleteCampaignItem`) removes item + entity — both `primeCampaignEntities` so the Codex stays in sync. **Edit (#505):** a per-item "Edit" affordance re-opens the *same* form pre-filled from the item (`formFromItem`, clone dropdown suppressed) and saves via `updateCampaignItem`; a rename is mirrored onto the fronting entity in the cache (`renameInCache`) and existing holders are preserved across the save. **Award/revoke (#381):** each item row also carries an "Award to \" picker (`awardCampaignItem` — grants into that member character's inventory; the panel takes `characters` from the parent campaign) and, per current holder, a "Revoke" button (`revokeCampaignItem`); awarding reveals the fronting entity locally. Holders (`{characterName, quantity}`) render inline under each item. **Capabilities & attunement (#546):** the Magic fieldset (shown only for a non-Mundane rarity) adds an **attunement prerequisite** selector (`ATTUNEMENT_PREREQ_OPTIONS`: anyone/class/spellcaster/species/alignment + a value input for the keyed kinds, gated on `requiresAttunement`) and `CapabilityEditor` — add/remove multiple `passiveBonus` capabilities, each a `{target, op, value|dice, condition, description}` row. `buildInput` sends `attunementPrereqKind/Value` (null-cleared when not applicable) and the full `capabilities[]` (REPLACE semantics server-side; `[]` clears). `formFromItem` round-trips both on Edit.
- **`features/entities/CapabilityEditor.tsx`** (#546) — the DM capability authoring sub-form used inside `CampaignItemsPanel`'s Magic fieldset (kinds: passiveBonus / castSpell / grant / **charges pool** #555; activatedEffect not yet authorable). **Decomposed (#604):** the editor is a thin orchestrator (add/remove + spell-catalog fetch) that maps each capability to a `CapabilityRow` dispatcher, which renders the per-kind field subcomponent — `CastSpellFields` (→ `CastResourceFields` / `CastStatSection` → `CastStatFields`), `PassiveBonusFields` (→ `DiceValueInput`), `GrantFields` (→ `AdvantageGrantFields` → `AdvantageKeyField` / `ProficiencyGrantFields`), `ChargesFields` — all in `features/entities/`. Draft defaults + field normalization live JSX-free in `lib/capabilityDraft`. passiveBonus rows: a **target** `Select` (`CAPABILITY_TARGET_OPTIONS`), a **key** `Select` for targets that name an ability/skill (`ABILITY_OPTIONS`/`SKILL_OPTIONS` — resolved through the helpers, never a raw key), an **op** `Select` (add/setTo), and either a scalar value or a **dice value** (count/faces/damage-type, toggled per row) for a `+2d6 fire`-style bonus. **Charges pool** rows (#555): max charges + a recharge-trigger `Select` (`CHARGE_TRIGGER_OPTIONS`: dawn/dusk/short/long) + a "Roll to regain" toggle exposing count/faces/bonus dice inputs (unchecked = refills to max); a castSpell row whose Resource is "Spends item charges" swaps its "Uses per period" input for **"Charges per cast"** (`chargeCost`). The server enforces ≤1 pool per item and that a charges-costed cast has a pool. **castSpell** rows fetch the spell catalog on demand and gate their DC/attack authoring fields on the referenced spell's `attackType`: save spells (e.g. Fireball) show **Save DC** only, attack spells (e.g. Fire Bolt) show **Attack bonus** only, utility/buff spells (e.g. Fly) show neither (and neither before a spell is picked). Picking a spell (`pickSpell` in `CastSpellFields`, delegating to `applySpell` in `lib/capabilityDraft`) clears the inapplicable value so no stale DC/attack persists, and the "Wielder DC/attack" hint is suppressed until at least one of those fields is visible. A live `capabilitySummary` line previews each row. All display via `lib/capabilities`; draft defaults + normalization via `lib/capabilityDraft`.
- **`features/entities/CampaignItemCard.tsx`** (#380) — the Codex item card rendered on `EntityDetailPage` for an ITEM entity (loaded via `fetchCampaignItemByEntity`): rarity/attunement/unique badges, category-specific mechanical detail, description, and (once awarded, #381) a **Held-by** holder list. `dmNotes` renders **only** when `isOwner` — and is never in a player payload to begin with (server-scrubbed).

Type display/tone resolve through `lib/mentions` (`ENTITY_TYPE_LABELS` / `ENTITY_TYPE_OPTIONS` / `ENTITY_TYPE_TONE`) — never capitalize the raw enum key.

`CampaignDetailPage` hosts the tabs as **routed** `Tabs` (#367), via the extracted `CampaignTabs` (strip + tab-click navigation) and `CampaignTabPanels` (active-tab content switch) subcomponents: `/campaigns/:id` = Overview (`features/campaign/CampaignOverviewPanel.tsx` — invite/add-character/roster), `/campaigns/:id/codex` = Codex (the `main` drops its `max-w-4xl` on this route so the ledger runs full-width, #840 — the tab strip keeps its own centered `max-w-4xl` wrapper), and `/campaigns/:id/manage` = the **owner-only** Manage tab (#379 — the tab is rendered only for the owner and a non-owner deep-linking to `/manage` is redirected back to Overview once the role resolves). The active tab derives from the URL (`useMatch`) and tab clicks `navigate` (push, not replace), so deep-links, refresh, and browser back/forward all work.

## Campaign sessions — `features/session/`

Sessions are **campaign-level** (#245): one shared session per play night that party members join and leave. The `CharacterSheetPage` header button is driven by `character.campaignId` + the campaign's active session (from `fetchActiveSession`) + whether this character is an active participant: no campaign → a "Join a campaign" link to `/campaigns`; an active session this character is in → "Resume Session"; an active session it isn't in → "Join Session" (`joinSession` then navigate); no active session → "Start Session" (`startCampaignSession`). `SessionPage` adds a "Leave Session" affordance (`leaveSession`) next to "End Session" (`endSession`, campaign-scoped); leaving returns to the sheet and keeps the session open for the rest of the party (it auto-closes server-side an hour after the last member leaves). **DM quick-award (#382):** when the viewer owns the campaign (role from `fetchCampaign`), `SessionPage` shows an owner-only **Loot** tab rendering `SessionLootPanel` — pick a participant, one-click-award any campaign item (`awardCampaignItem` with the live `sessionId`); the loot event lands in the Log tab (recipient named) and the end-of-session recap's Loot line.

`SessionPage` (in `pages/`) is a thin loader shell — it resolves the character + active session, then hands off to **`SessionContent`** (`features/session/`, #613), the live-play orchestrator that owns the page async state (end/leave/XP-award, owner check, log refresh) and composes the presentational pieces: `SessionTopBar` (title bar + Note/Leave/End controls), `TurnHub`, `SessionReferenceTabs` (the tab bar + Inventory/Spells/Class/Log/Loot panels — the **Rest & HP tab is gone** (#814); rests moved to the always-visible `RestButton` beside `CompactHpBar` and HP to that strip's sheet — tab visibility + the gated-away fallback (a persisted `rest` selection falls back to Inventory) come from `lib/sessionTabs`), and `SessionOverlays` (the End-Session prompt + recap modal + capture palette). The turn-slot dispatch inside `useTurnActions.handleActionClick` plans via `lib/turnActionPlan`. **Server-effect undo (#758):** a server-effect action (Second Wind, Rage, …) tags the audit `batchId` returned by `applyActionTransactions` onto its `useTurnState` history entry (`attachBatchId`); the Undo button routes through `useTurnActions.handleUndo`, which for a batch-carrying entry calls `revertBatch` **before** popping the local slot (a local-only entry — Dodge, attack-mode — just pops). A 4xx from revert (the batch isn't the most-recent non-reverted one) surfaces the error and leaves the slot consumed — no client/server desync. **Deferred picker commit (#765):** the item and Lay on Hands sheets extend this — `TurnResolutionSheets` threads an `onCommit` into `InlineItemPicker` (fires after a successful `applyActionTransactions`: `consumeAction` + `attachBatchId(batchId)`) and `LayOnHandsInput` (`onCommit` = `consumeAction`, invoked **before** `onSend` so `send`'s own `attachBatchId` lands on that history entry — same ordering as Second Wind); closing either sheet is a plain `closeResolution` and costs nothing. **Mid-turn weapon change (#733 / picker redesign #789 / re-homed into the Action sheet #815):** the loadout swap lives inside the Action picker, not a slot-root row. `ActionSheetBody` renders a **"Change weapons"** `OptionCard` (near "Use an item") whose subtitle is `equippedLoadoutLabel` + the cost copy ("only a held-weapon swap costs the Action; a free-hand draw or stow is free"). Picking it dispatches `handleActionClick("changeWeapons")` → the `changeWeapons` resolver (`kind: "loadout-picker"`, `serverEffect: false`); `lib/turnActionPlan` plans it `{ consumeSlot: false, openResolution: true, send: "none" }` (the swap owns the Action itself, like the item/spell pickers), so `TurnResolutionSheets` opens `InlineLoadoutPicker` in a `BottomSheet` — a **Main-hand and Off-hand card**, each expanding an inline disclosure of bag candidates deduped by name (×N badge) plus a free **Stow**. `useLoadoutSwap` is **hoisted into `useTurnActions`** (so the sheet banner and the under-slot Refund share one committed-swap state) and posts the `setEquipped`+`equip` batch, spending the **Action** (`consumeAction`) only when replacing an occupied hand (drawing into a free hand is free; the `:86` guard stays as the server-of-truth backstop; no-op re-selecting current); `stow()` empties a hand for free. Gating is computed **up front** in `lib/loadoutPicker` (`handContext`/`handPickerOptions`/`handButtonDisabledReason`, JSX-free + unit-tested): at 0 actions an occupied hand's Change and any option that must stow a held item (including a two-handed weapon needing the off-hand free) render **disabled with a text reason** (not title-only, so Axe passes), while a free hand's Equip stays enabled. There is **no row-level `noLegalMove` gate**; the "Change weapons" card lives in the Action sheet while an action remains, and once the Action is spent the `ActionSlot` renders a free-move **"Change weapons" button** that opens the same picker — so free-hand draws/stows stay reachable at 0 actions (matching the old always-visible loadout row). A committed swap **keeps the sheet open** showing the result + a **Refund**, and `LoadoutRefundStrip` surfaces the same Refund under the turn slots until it's refunded or the turn ends (`refundAction` = the `grantExtraAction` +1, plus the inverse inventory batch) — the local undo can't reverse a server-committed swap. `turnState.twfAvailable` is **derived** (not persisted) from the live loadout, so a mid-turn swap into/out of two light weapons updates the off-hand affordance immediately. **Attack cantrips (#734):** `InlineAttackPicker` renders an `InlineSpellAttackSection` listing `isAttackCantrip` spells (Fire Bolt); each is a two-step "Attack `{+bonus}`" (d20 via `spellcasting.spellAttackBonus`, gates Cast) → "Cast" (damage via `computeCastSpec`, one `castSpell` transaction, logs both rolls). It **branches the economy away from the weapon Extra-Attack counter** — never `recordAttack`; on cast it calls `commitActionSpell(0)` (spends the action, tears down attack mode, records `spellCastThisTurn.action` for the leveled-spell interlock). `attackType:"save"` cantrips (Sacred Flame) stay in the normal spell picker. **Multi-attack tally + verdict flow (#802/#811):** `useTurnState` carries an `attackTally: AttackTallyRow[]` for the current turn's swings (persisted in the localStorage snapshot; `hydrateTurnState` backfills it empty for pre-#802 entries and mints `id`/defaults `source: "action"` on each row for pre-#813 entries, top-level + undo history; snapshotted **inside** `EconomySnapshot` so undoing a `recordAttack`/`recordTwfAttack` drops its row). `recordAttack(recorded)` appends an `action`-source row with the kept-d20 snapshot + `autoVerdict` (`recordTwfAttack(recorded)` is the off-hand twin — appends a `bonusAction`-source row, undoable, so the off-hand swing lands in the same banner, #813); `setTallyDamage(rowId, dmg)`/`addTallyDamageRider(rowId, amt)` write/replace (never append) **that row's** single damage slot **and auto-resolve an unset verdict to `hit`** (implicit hit — rolling damage IS the hit call, #811) — targeting by `rowId` (not "the last row") so two interleaved sources never misattribute (#813); `setTallyDamageAt(i, dmg)` is the index-addressed variant the banner's inline resolve uses; `setTallyAttackTotal(rowId, total)` overrides just that row's to-hit total after a Precision Attack die is added (#809) — it leaves `keptFace`/`nat20`/`nat1` untouched so the verdict (crit/miss) never reads the boosted total; `setTallyVerdict(i, verdict)` writes a verdict directly (replaced #802's tap-cycle; nat-locked rows refuse; switching to miss drops the row's damage). The tally clears **per source**: `enterAttackMode` drops only `action` rows (a NEW Attack action), `enterTwfMode` drops only `bonusAction` rows, and `endTurn` + `clearAttackTally` (banner dismiss — which also drops tally rows from undo-history snapshots, #812, so undo can't resurrect a dismissed banner). **The attack sheet is one step-rail card** (`AttackStepCard`, #811 — merged the old AttackFormCard + WeaponDamageCard): *Roll to hit → Call it → Damage* with done/active/pending dots from `lib/attackStepRail`. The verdict is the **single crit authority** — "it Missed" / "Crit!" buttons sit on the to-hit result line ("Does 19 hit? Ask your DM."), nat 20/nat 1 lock the call, the old Crit checkbox and `useAttackRolls.manualCrit` are gone, and `useAttackRolls` reads the current row's verdict (the off-hand sheet now records real tally rows too, so its verdict flow is identical — #813) to double damage dice. A manual miss resets the card and arms "Roll to hit — attack N of M"; a resolved hit keeps it expanded with a full-width "Next" button that re-arms step 1 rather than instantly re-rolling (`onNext` → `markRolled(null)`), giving the player a beat to re-orient (e.g. switch attack forms) before the next roll — a two-tap flow (#834); the quiet "Skip — roll next attack ›" link is the ungated path that produces **unresolved** rows. The rule everywhere is *unresolved = tappable, resolved = final*: `AttackTallyStrip` ("This action") is display-only except a dotted-underline "hit or miss?" on unresolved rows (expands Hit/Miss/Crit! in place), and the Turn-summary banner resolves inline via `useTallyResolve` — tapping an unresolved line expands Hit/Miss/Crit!, choosing Hit/Crit grows an on-line "Roll damage" button (damage spec derived at resolve time from the row's `formId` via `buildAttackForms` — no persisted spec; 3D dice + session log + `setTallyDamageAt`), and tapping a *resolved* line reveals a quiet "Change · …" row (never on nat-locked lines). At `md`+ the attack sheet widens (`BottomSheet wide` → 42rem) and the counter kicker+pips (`AttackKickerPips`), tally strip, maneuvers disclosure, and attack cantrips move into a right rail beside the step card (placement via `useIsBelowMd`, one mount per widget). Closing the sheet with attacks unspent leaves the action **live** (`cancelAttack` is a no-op post-first-roll) so `ActionSlot` shows "Resume attack — N of M remaining" (`handleResumeAttack` reopens the sheet with no economy change) and the footer reads "Close"; `finishAttack` fires only when all attacks are spent or the turn ends. Once the sheet is closed and rows exist, `TurnHub` renders the "Turn summary" banner (`TurnSummaryBanner`, renamed from "Tell your DM" in #812) below the slots (one line per attack; Dismiss calls `clearAttackTally`, so dismissal survives a reload of the persisted snapshot). **Maneuver hosting (#809/#811):** the Battle Master `ManeuverPrompt` halves (`section="attack"` — Precision, keyed on the form id so a form switch resets spend state per #756 — and `section="damage"` — Trip/Menacing… after a damage roll) plus `AttackOptionSection` (Commander's Strike) all live behind `ManeuversDisclosure`, a single 44px collapsed row ("Battle Master maneuvers · d8 × 4"). A Precision spend flows through `useAttackRolls.makeOnRollsUpdated` → `setAttackTotals` (the on-card result line) **and** `setTallyAttackTotal(rowId, …)` (the current source's tally row + Turn-summary banner). The off-hand sheet mounts the same `ManeuversDisclosure` (RAW: maneuvers apply to any weapon attack, #813) — its superiority-die adds flow through the shared `useAttackRolls` path. **Rich option-card pickers:** the Action/Bonus/Reaction sheets render `OptionCard`s (row/half/tile variants; `ariaLabel` defaults to the bare title so accessible names stay stable) instead of pill buttons, fed by the `ActionSheetModel`/`BonusSheetModel`/`ReactionSheetModel` render models `useTurnActions` builds from `lib/turnOptions` — slots stay presentational and `character` never flows into them. The Action sheet (`ActionSheetBody`) shows Attack (equipped-weapon summary), Cast a spell (hidden for non-casters), Use an item (consumable-count badge), **Change weapons** (loadout summary; opens `InlineLoadoutPicker`, #815; always visible — the picker owns its empty state, same as Use an item at zero consumables), class actions (pool badges, heal subtitles), a Dash/Dodge half-pair, and a **"More actions" disclosure** that expands in place to a 3-col tile grid (Disengage/Hide/Help/Search/Ready/Grapple/Shove). The Bonus sheet (`BonusActionSheetBody`) lists each **castable bonus-action spell as its own card** — tapping calls `handleBonusSpellCast(spellId)` which opens the spell resolution via `openResolution("castSpellBonus", { spellId })` (a `ResolutionContext` on `useActiveResolution`); `InlineSpellPicker`'s `focusSpellId` prop then renders just that spell's row with a "Show all spells" escape hatch. A footer `twfHint` names the light-weapon pair to equip when TWF is unavailable. The Reaction sheet includes a neutral "Other reaction" catch-all (`consumeReaction`, mirrors "Other bonus action"). The cast sheet itself groups spells into level sections with gold slot pips (+ sr-only counts) and hides unaffordable levels behind the `hiddenLevelsNote` footer. **Mobile turn shell (#746):** during the player's active turn (`turnState.phase === "active"`) on a `< md` viewport, `SessionHeaderRegion` swaps `SessionTopBar` for the slim `CompactTurnHeader` (back chevron + name + `race class (subclass) · Level N` + a Round chip, with Note/Leave/End collapsed into `OverflowMenu`) and `SessionContent` hides `SessionReferenceTabs` — so the turn reads as one clean screen. Pure CSS `md:` toggles gated on that phase flag (`hidden md:block` / `md:hidden`), no JS breakpoint hook; the full bar + tabs return at `md`+ and between turns. **Tap-to-manage HP (#768):** the always-visible `CompactHpBar` is a labeled button ("Manage hit points", chevron + hover/active tint + a mobile "tap to manage" hint) that opens a "Hit Points" `BottomSheet` (`HpSheetBody`) — so HP is manageable mid-turn on mobile without a dedicated tab. Beside it, an always-visible **`RestButton`** (campfire/moon, #814) opens a "Rest" `BottomSheet` with `RestControls` (short-rest hit-die spend + long rest) plus the hit-dice readout; both surfaces are always visible, mirroring `CompactHpBar` (no active-turn gating). `RestButton` and the character-sheet `HitPointTracker` share the JSX-free **`useRestActions`** hook (client-side per-die rolls → `shortRest {rolls}` / `longRest` via `applyHitPointOperations`). The HP surfaces share `useHitPointApply` (submit + concentration-check surfacing), so damage/heal/temp + the concentration banner/roll-modal behave identically; ops route through `applyHitPointOperations`, and the returned character flows out via `handleCharacterUpdate` — the same path `useTurnState`'s server-HP watcher reads, so sheet damage still flags `tookDamageThisTurn` (Rage auto-end). Death saves stay on the turn screen (`TurnDeathSaves`), not in the sheet. **Compact conditions strip (#769):** the conditions surface follows the same split — `SessionContent` renders `CompactConditionsBar` (`md:hidden`; a labeled "Manage conditions" button showing active-condition chips + an "Exhaustion N" chip, or a muted "No conditions", that opens a "Conditions" `BottomSheet`) on mobile and the full `ConditionsStrip` card (`hidden md:block`) at `md`+. Both mount the shared `ConditionsSheetBody` (chips/remove + exhaustion stepper + `AddConditionPanel`, owning `applyConditionTransactions`/busy/error), so applies/removes route through the same path and reflect on the strip immediately. The character sheet keeps the full `ConditionsStrip` card unconditionally.

Client functions (all in `client.ts`): `startCampaignSession`, `joinSession`, `leaveSession`, `endSession`, `fetchCampaignSessions` are campaign-scoped; `fetchActiveSession`, `fetchSessions`, `fetchSession` stay character-scoped reads. `SessionsModal` lists the **campaign's** session history (`fetchCampaignSessions`, keyed by `campaignId`); `SessionSummaryModal` renders the campaign recap aggregate up top (XP/spells/rolls/combat + party size, items split into **acquired** vs **sold** vs **loot** (DM-awarded, #382) sections, plus slots-spent and feats/ASIs) then a per-participant card (name, time present, stat tiles, per-member items/slots/feats) for each member — **only when there's more than one participant** (a solo session shows the aggregate alone, since the single card would just duplicate it). Below that, the session's journals rendered inline via `MentionText` (@-chips resolved), and the retroactive "add XP to this session" form (which refreshes the participant summary + recap in place).

### Durable-buff end-conditions — `lib/turnHooks.ts`

Durable (`while-active`) buffs like Rage (#457/#458) auto-end from the session turn tracker via a small data-driven registry, not tracker-hardcoded logic. `DURABLE_BUFF_END_CONDITIONS` maps each buff `key` → `{ endActionKey, reminder, endsWhen(window) }`: `endActionKey` is the action the tracker POSTs to clear the buff server-side (Rage → `endRage`), `reminder` is the "when/why it ends" note the turn UI surfaces (Rage's also carries the Str-checks/saves-advantage note pointing at the #459 roll-mode toggle), and `endsWhen(TurnActivityWindow)` decides from this turn's activity (did they attack? take damage?). `useTurnActions.handleEndTurn` calls `buffsToAutoEnd(activeBuffKeys, window)` to fire the clears and `endReminders(activeBuffKeys)` to render the active-buff banner. Adding a new subclass durable buff (Evasive Footwork, Defensive Duelist per the #416 epic) is a one-entry addition here — no tracker edit.
