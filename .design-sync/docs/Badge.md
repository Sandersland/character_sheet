---
category: Primitives
---

Soft-background, fully-rounded badge — the safe default for status and
category tags. Used for class/level chips, spell-school tags, and the backend
health indicator. Color never carries meaning on its own; the text label
always states the status. Five tones map to the palette: `neutral` (default),
`garnet`, `arcane`, `gold`, and `vitality`.
