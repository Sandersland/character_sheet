---
category: Primitives
---

Base surface used throughout the app: a parchment panel with a soft card
shadow and the single shared corner radius (`--radius-card`). Optionally
renders a small uppercase caption heading; `titleAccessory` fills the right
edge of that title row (e.g. a `Badge`). Compose any content as `children`.
